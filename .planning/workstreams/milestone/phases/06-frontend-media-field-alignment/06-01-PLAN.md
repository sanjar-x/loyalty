---
phase: 06-frontend-media-field-alignment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/admin/src/services/products.js
  - frontend/admin/src/hooks/useSubmitProduct.js
  - frontend/admin/src/app/api/catalog/products/[productId]/media/upload/route.js
  - frontend/admin/src/app/api/catalog/products/[productId]/media/[mediaId]/confirm/route.js
  - frontend/admin/src/app/api/catalog/products/[productId]/media/external/route.js
autonomous: true
requirements: [MEDIA-01, MEDIA-02]

must_haves:
  truths:
    - "reserveMediaUpload calls /api/media/upload (not /api/catalog/products/{id}/media/upload)"
    - "confirmMedia calls /api/media/{storageObjectId}/confirm (not /api/catalog/products/{id}/media/{mediaId}/confirm)"
    - "addExternalMedia calls /api/media/external (not /api/catalog/products/{id}/media/external)"
    - "Upload response is read as slot.presignedUrl (not slot.presignedUploadUrl)"
    - "Upload response is read as slot.storageObjectId (not slot.id)"
    - "Upload request sends { contentType, filename } (not { mediaType, role, contentType, sortOrder })"
    - "External import request sends { url } (not { externalUrl, mediaType, role, sortOrder })"
    - "Old broken BFF route files are deleted"
  artifacts:
    - path: "frontend/admin/src/services/products.js"
      provides: "Corrected media service functions with new BFF routes"
      contains: "/api/media/upload"
    - path: "frontend/admin/src/hooks/useSubmitProduct.js"
      provides: "Corrected orchestration using new service signatures and response fields"
      contains: "slot.presignedUrl"
  key_links:
    - from: "frontend/admin/src/hooks/useSubmitProduct.js"
      to: "frontend/admin/src/services/products.js"
      via: "import { reserveMediaUpload, confirmMedia, addExternalMedia }"
      pattern: "reserveMediaUpload\\("
    - from: "frontend/admin/src/services/products.js"
      to: "/api/media/upload"
      via: "fetch call in reserveMediaUpload"
      pattern: "api\\('/api/media/upload'"
    - from: "frontend/admin/src/services/products.js"
      to: "/api/media/{id}/confirm"
      via: "fetch call in confirmMedia"
      pattern: "api\\(`/api/media/"
    - from: "frontend/admin/src/services/products.js"
      to: "/api/media/external"
      via: "fetch call in addExternalMedia"
      pattern: "api\\('/api/media/external'"
---

<objective>
Fix admin frontend media service functions and orchestration hook to use the correct BFF routes (created in Phases 4+5), correct request schemas, and correct response field names from image_backend. Delete the old broken product-scoped BFF route files.

Purpose: Without this fix, the media upload/confirm/external flow is completely broken -- service functions call non-existent main-backend routes with wrong field names, causing 404s or 422s. This phase makes the media pipeline functional.

Output: Working media service layer pointing at image_backend BFF routes with correct request/response schemas.
</objective>

<execution_context>
@C:/Users/Sanjar/Desktop/loyality/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Sanjar/Desktop/loyality/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/milestone/ROADMAP.md
@.planning/workstreams/milestone/STATE.md
@.planning/workstreams/milestone/phases/06-frontend-media-field-alignment/06-RESEARCH.md

<interfaces>
<!-- BFF routes already created in Phases 4+5. These are the endpoints the service functions must call. -->

From frontend/admin/src/app/api/media/upload/route.js (Phase 4):
- POST /api/media/upload
- Accepts JSON: { contentType: string, filename?: string }
- Returns 201: { storageObjectId: string, presignedUrl: string, expiresIn: number }

From frontend/admin/src/app/api/media/[id]/confirm/route.js (Phase 5):
- POST /api/media/{storageObjectId}/confirm
- No body
- Returns 202: { storageObjectId: string, status: string }

From frontend/admin/src/app/api/media/external/route.js (Phase 5):
- POST /api/media/external
- Accepts JSON: { url: string }
- Returns 201: { storageObjectId: string, url: string, variants: [...] }

From frontend/admin/src/services/products.js (current helper):
```javascript
async function api(url, options = {}) { ... }
function jsonOpts(body) { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite media service functions and delete old routes</name>
  <files>
    frontend/admin/src/services/products.js,
    frontend/admin/src/app/api/catalog/products/[productId]/media/upload/route.js,
    frontend/admin/src/app/api/catalog/products/[productId]/media/[mediaId]/confirm/route.js,
    frontend/admin/src/app/api/catalog/products/[productId]/media/external/route.js
  </files>
  <read_first>
    frontend/admin/src/services/products.js,
    frontend/admin/src/app/api/media/upload/route.js,
    frontend/admin/src/app/api/media/[id]/confirm/route.js,
    frontend/admin/src/app/api/media/external/route.js
  </read_first>
  <action>
In `frontend/admin/src/services/products.js`, rewrite the three media service functions (lines 63-87). Keep all other functions (api, jsonOpts, createProduct, bulkAssignAttrs, generateSkus, listSkus, updateSku, uploadToS3, changeProductStatus) UNCHANGED.

Replace `reserveMediaUpload` (line 63-65):
```javascript
export async function reserveMediaUpload(payload) {
  // payload = { contentType, filename }
  return api('/api/media/upload', jsonOpts(payload));
}
```
- Remove `productId` parameter (image_backend is product-agnostic)
- Route changes from `/api/catalog/products/${productId}/media/upload` to `/api/media/upload`
- Payload is { contentType, filename } -- BFF route strips anything else anyway

Replace `confirmMedia` (line 81-83):
```javascript
export async function confirmMedia(storageObjectId) {
  return api(`/api/media/${storageObjectId}/confirm`, { method: 'POST' });
}
```
- Remove `productId` parameter
- Rename `mediaId` to `storageObjectId` for clarity
- Route changes from `/api/catalog/products/${productId}/media/${mediaId}/confirm` to `/api/media/${storageObjectId}/confirm`

Replace `addExternalMedia` (line 85-87):
```javascript
export async function addExternalMedia(payload) {
  // payload = { url }
  return api('/api/media/external', jsonOpts(payload));
}
```
- Remove `productId` parameter
- Route changes from `/api/catalog/products/${productId}/media/external` to `/api/media/external`

Do NOT modify `uploadToS3` (line 67-79) -- its signature `(presignedUrl, file)` is already correct. The bug is in the callsite passing the wrong field, which Task 2 fixes.

Then DELETE these three old broken BFF route files (replaced by Phase 4+5 routes):
1. `frontend/admin/src/app/api/catalog/products/[productId]/media/upload/route.js`
2. `frontend/admin/src/app/api/catalog/products/[productId]/media/[mediaId]/confirm/route.js`
3. `frontend/admin/src/app/api/catalog/products/[productId]/media/external/route.js`

Also delete the now-empty parent directories:
- `frontend/admin/src/app/api/catalog/products/[productId]/media/upload/`
- `frontend/admin/src/app/api/catalog/products/[productId]/media/[mediaId]/confirm/`
- `frontend/admin/src/app/api/catalog/products/[productId]/media/[mediaId]/`
- `frontend/admin/src/app/api/catalog/products/[productId]/media/external/`
- `frontend/admin/src/app/api/catalog/products/[productId]/media/`
- `frontend/admin/src/app/api/catalog/products/[productId]/` (only if empty after media removal)
- `frontend/admin/src/app/api/catalog/products/` (only if empty)
- `frontend/admin/src/app/api/catalog/` (only if empty)

Check if each parent directory is empty before deleting -- there may be other routes under /api/catalog/.
  </action>
  <verify>
    <automated>cd /c/Users/Sanjar/Desktop/loyality/frontend/admin && grep -n "api/media/upload" src/services/products.js && grep -n "api/media/external" src/services/products.js && grep -c "productId" src/services/products.js | grep -v "// old" && test ! -f "src/app/api/catalog/products/[productId]/media/upload/route.js" && echo "PASS: old route deleted" && test ! -f "src/app/api/catalog/products/[productId]/media/[mediaId]/confirm/route.js" && echo "PASS: old confirm deleted" && test ! -f "src/app/api/catalog/products/[productId]/media/external/route.js" && echo "PASS: old external deleted"</automated>
  </verify>
  <acceptance_criteria>
    - grep "/api/media/upload" in products.js finds reserveMediaUpload function
    - grep "/api/media/external" in products.js finds addExternalMedia function
    - grep "storageObjectId" in products.js finds confirmMedia parameter
    - reserveMediaUpload signature has NO productId parameter (grep "reserveMediaUpload(payload)" succeeds)
    - confirmMedia signature has NO productId parameter (grep "confirmMedia(storageObjectId)" succeeds)
    - addExternalMedia signature has NO productId parameter (grep "addExternalMedia(payload)" succeeds)
    - Old route file at catalog/products/[productId]/media/upload/route.js does NOT exist
    - Old route file at catalog/products/[productId]/media/[mediaId]/confirm/route.js does NOT exist
    - Old route file at catalog/products/[productId]/media/external/route.js does NOT exist
  </acceptance_criteria>
  <done>
    Three media service functions in products.js point to new BFF routes (/api/media/*) with correct signatures (no productId). Three old broken route files deleted.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix media orchestration callsites in useSubmitProduct.js</name>
  <files>frontend/admin/src/hooks/useSubmitProduct.js</files>
  <read_first>
    frontend/admin/src/hooks/useSubmitProduct.js,
    frontend/admin/src/services/products.js
  </read_first>
  <action>
In `frontend/admin/src/hooks/useSubmitProduct.js`, update ALL callsites for the three media service functions to use new signatures and correct response field names. There are TWO blocks that need fixing: main image upload (lines 127-160) and size guide upload (lines 163-187).

IMPORTANT: Keep `role` and `sortOrder` in the `uploadTasks` array mapping (line 120-124) -- they are needed for future "attach to product" step (Phase 7/8). Just do NOT send them to image_backend.

**Fix 1 -- Main image upload block (inside chunk.map callback, lines 131-147):**

Replace the external URL branch (lines 131-137):
```javascript
if (image.source === 'url') {
  await addExternalMedia({ url: image.url });
}
```
- Remove `productId` first argument
- Change payload from `{ externalUrl: image.url, mediaType: 'image', role, sortOrder }` to `{ url: image.url }`
- image_backend ExternalImportRequest only accepts `{ url }`

Replace the file upload branch (lines 138-147):
```javascript
} else if (image.file) {
  const slot = await reserveMediaUpload({
    contentType: image.file.type || 'image/jpeg',
    filename: image.file.name,
  });
  await uploadToS3(slot.presignedUrl, image.file);
  await confirmMedia(slot.storageObjectId);
}
```
- Remove `productId` first argument from reserveMediaUpload
- Change payload from `{ mediaType: 'image', role, contentType: ..., sortOrder }` to `{ contentType: ..., filename: image.file.name }`
- Add `filename: image.file.name` (from browser File API)
- Fix `slot.presignedUploadUrl` to `slot.presignedUrl` (MEDIA-01)
- Fix `slot.id` to `slot.storageObjectId` (MEDIA-01)
- Remove `productId` first argument from confirmMedia

**Fix 2 -- Size guide upload block (lines 165-183):**

Replace the external URL branch (lines 167-173):
```javascript
if (sizeGuide.source === 'url') {
  await addExternalMedia({ url: sizeGuide.url });
}
```
- Remove `productId` first argument
- Change payload from `{ externalUrl: sizeGuide.url, mediaType: 'image', role: 'size_guide', sortOrder: 0 }` to `{ url: sizeGuide.url }`

Replace the file upload branch (lines 174-183):
```javascript
} else if (sizeGuide.file) {
  const slot = await reserveMediaUpload({
    contentType: sizeGuide.file.type || 'image/jpeg',
    filename: sizeGuide.file.name,
  });
  await uploadToS3(slot.presignedUrl, sizeGuide.file);
  await confirmMedia(slot.storageObjectId);
}
```
- Same fixes as main upload: remove productId, fix payload, fix response fields

After all changes, verify the file still has correct import list at top (line 4-15). The import names do NOT change -- only the callsites change.
  </action>
  <verify>
    <automated>cd /c/Users/Sanjar/Desktop/loyality/frontend/admin && ! grep -n "presignedUploadUrl" src/hooks/useSubmitProduct.js && echo "PASS: no presignedUploadUrl" && ! grep -n "slot\.id" src/hooks/useSubmitProduct.js && echo "PASS: no slot.id" && grep -c "slot.presignedUrl" src/hooks/useSubmitProduct.js && grep -c "slot.storageObjectId" src/hooks/useSubmitProduct.js && grep -c "filename:" src/hooks/useSubmitProduct.js && ! grep -n "externalUrl:" src/hooks/useSubmitProduct.js && echo "PASS: no externalUrl field" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep "presignedUploadUrl" in useSubmitProduct.js returns NO matches (old field name gone)
    - grep "slot.id" in useSubmitProduct.js returns NO matches (old field name gone)
    - grep "slot.presignedUrl" in useSubmitProduct.js returns 2 matches (main upload + size guide)
    - grep "slot.storageObjectId" in useSubmitProduct.js returns 2 matches (main upload + size guide)
    - grep "filename:" in useSubmitProduct.js returns 2 matches (main upload + size guide)
    - grep "externalUrl:" in useSubmitProduct.js returns NO matches (replaced with url:)
    - grep "{ url:" in useSubmitProduct.js returns 2 matches (main external + size guide external)
    - The uploadTasks array still computes `role` and `sortOrder` (grep "role:" returns matches)
    - npm run build succeeds without errors
  </acceptance_criteria>
  <done>
    All media callsites in useSubmitProduct.js use correct service signatures (no productId), correct request fields (contentType + filename, url), and correct response fields (presignedUrl, storageObjectId). Build passes.
  </done>
</task>

</tasks>

<verification>
1. `grep -n "api/media/" frontend/admin/src/services/products.js` -- shows 3 functions using new BFF routes
2. `grep -n "presignedUrl\|storageObjectId\|filename" frontend/admin/src/hooks/useSubmitProduct.js` -- shows correct field names
3. `! grep -rn "presignedUploadUrl\|externalUrl:" frontend/admin/src/hooks/useSubmitProduct.js frontend/admin/src/services/products.js` -- no old field names remain
4. `test ! -d frontend/admin/src/app/api/catalog/products/[productId]/media` -- old route directory gone
5. `cd frontend/admin && npm run build` -- build succeeds with no errors
</verification>

<success_criteria>
- Admin frontend media service functions call /api/media/* BFF routes (not /api/catalog/products/*/media/*)
- Upload request sends { contentType, filename } only
- External import request sends { url } only
- Response fields read as presignedUrl and storageObjectId
- Old broken route files deleted
- Admin build passes
</success_criteria>

<output>
After completion, create `.planning/workstreams/milestone/phases/06-frontend-media-field-alignment/06-01-SUMMARY.md`
</output>
