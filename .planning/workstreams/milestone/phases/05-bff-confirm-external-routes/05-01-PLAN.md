---
phase: 05-bff-confirm-external-routes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/admin/src/app/api/media/[id]/confirm/route.js
  - frontend/admin/src/app/api/media/external/route.js
autonomous: true
requirements: [BFF-03, BFF-04]

must_haves:
  truths:
    - "POST /api/media/{id}/confirm proxies to image_backend POST /api/v1/media/{id}/confirm and returns 202"
    - "POST /api/media/external proxies to image_backend POST /api/v1/media/external and returns 201"
    - "Both routes use imageBackendFetch with X-API-Key (not backendFetch with JWT)"
    - "Both routes check user session via getAccessToken before proxying"
  artifacts:
    - path: "frontend/admin/src/app/api/media/[id]/confirm/route.js"
      provides: "BFF proxy for media confirm"
      contains: "imageBackendFetch"
    - path: "frontend/admin/src/app/api/media/external/route.js"
      provides: "BFF proxy for external media import"
      contains: "imageBackendFetch"
  key_links:
    - from: "frontend/admin/src/app/api/media/[id]/confirm/route.js"
      to: "image_backend POST /api/v1/media/{id}/confirm"
      via: "imageBackendFetch(`/api/v1/media/${id}/confirm`)"
      pattern: "imageBackendFetch.*api/v1/media.*confirm"
    - from: "frontend/admin/src/app/api/media/external/route.js"
      to: "image_backend POST /api/v1/media/external"
      via: "imageBackendFetch('/api/v1/media/external')"
      pattern: "imageBackendFetch.*api/v1/media/external"
---

<objective>
Create two BFF proxy routes in the admin Next.js app that forward media confirm and external import requests directly to image_backend using imageBackendFetch().

Purpose: The admin frontend currently has broken media routes nested under /api/catalog/products/[productId]/media/... that proxy through the main backend using backendFetch() with JWT. The main backend does NOT have these media endpoints — they only exist in image_backend. This plan creates the correct top-level /api/media/ routes that go directly to image_backend with X-API-Key auth.

Output: Two new route handler files that correctly proxy confirm and external import requests to image_backend.
</objective>

<execution_context>
@C:/Users/Sanjar/Desktop/loyality/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Sanjar/Desktop/loyality/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/milestone/ROADMAP.md
@.planning/workstreams/milestone/STATE.md

@.planning/workstreams/milestone/phases/03-bff-media-proxy-infrastructure/03-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From frontend/admin/src/lib/image-api-client.js:
```javascript
// Signature: imageBackendFetch(path, options = {})
// Returns: { ok: boolean, status: number, data: object|null }
// Auth: X-API-Key header added automatically from process.env.IMAGE_BACKEND_API_KEY
// Default header: Content-Type: application/json
// Error: Returns { ok: false, status: 502, data: { error: { code: 'IMAGE_BACKEND_UNAVAILABLE', message: '...', details: {} } } }
export async function imageBackendFetch(path, options = {}) { ... }
```

From frontend/admin/src/lib/auth.js:
```javascript
// Returns access_token cookie value or null
export async function getAccessToken() { ... }
```

image_backend confirm endpoint contract:
```
POST /api/v1/media/{storage_object_id}/confirm
Request: No body
Response 202: { "storageObjectId": "uuid", "status": "processing" }
Errors: 404 (not found), 422 (file not in S3)
```

image_backend external import endpoint contract:
```
POST /api/v1/media/external
Request: { "url": "https://example.com/image.jpg" }
Response 201: { "storageObjectId": "uuid", "url": "https://...", "variants": [...] }
Errors: 422 (download failed or file too large)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create BFF confirm route at /api/media/[id]/confirm</name>
  <files>frontend/admin/src/app/api/media/[id]/confirm/route.js</files>
  <read_first>
    - frontend/admin/src/lib/image-api-client.js
    - frontend/admin/src/lib/auth.js
    - frontend/admin/src/app/api/catalog/products/[productId]/media/[mediaId]/confirm/route.js
  </read_first>
  <action>
Create file `frontend/admin/src/app/api/media/[id]/confirm/route.js` with a single POST handler:

1. Import `NextResponse` from `next/server`, `imageBackendFetch` from `@/lib/image-api-client`, `getAccessToken` from `@/lib/auth`.

2. Export `async function POST(request, { params })` that:
   a. Calls `const token = await getAccessToken()` — if null, return `NextResponse.json({ error: { code: 'UNAUTHORIZED' } }, { status: 401 })`.
   b. Destructures `const { id } = await params` (MUST await — Next.js 16 async params).
   c. Calls `const { ok, status, data } = await imageBackendFetch(\`/api/v1/media/${id}/confirm\`, { method: 'POST' })`.
   d. Returns `NextResponse.json(data ?? { error: { code: 'SERVICE_UNAVAILABLE' } }, { status: ok ? 202 : (status || 502) })`.

No request body is read or forwarded — the confirm endpoint takes no body.
The getAccessToken check verifies the user is logged in (session check) but the token is NOT sent to image_backend — imageBackendFetch uses X-API-Key instead.

This follows the exact same pattern as the existing broken route at `src/app/api/catalog/products/[productId]/media/[mediaId]/confirm/route.js`, but replaces `backendFetch` with `imageBackendFetch` and removes the productId scoping.
  </action>
  <verify>
    <automated>cd "C:/Users/Sanjar/Desktop/loyality/frontend/admin" && node -e "import('./src/app/api/media/[id]/confirm/route.js').then(m => { if (typeof m.POST !== 'function') throw new Error('POST not exported'); console.log('OK: POST exported'); })" 2>/dev/null || (grep -q "export async function POST" "src/app/api/media/[id]/confirm/route.js" && grep -q "imageBackendFetch" "src/app/api/media/[id]/confirm/route.js" && grep -q "await params" "src/app/api/media/[id]/confirm/route.js" && grep -q "getAccessToken" "src/app/api/media/[id]/confirm/route.js" && echo "OK: all patterns found")</automated>
  </verify>
  <acceptance_criteria>
    - File exists at frontend/admin/src/app/api/media/[id]/confirm/route.js
    - File imports imageBackendFetch from @/lib/image-api-client (grep: "imageBackendFetch.*from.*@/lib/image-api-client")
    - File imports getAccessToken from @/lib/auth (grep: "getAccessToken.*from.*@/lib/auth")
    - File does NOT import backendFetch (grep -v: "backendFetch")
    - File uses await params (grep: "await params")
    - File forwards to /api/v1/media/${id}/confirm (grep: "api/v1/media/.*confirm")
    - File returns 202 on success (grep: "202")
    - File returns 401 if no token (grep: "401")
  </acceptance_criteria>
  <done>POST /api/media/{id}/confirm route handler exists, imports imageBackendFetch (not backendFetch), checks session via getAccessToken, forwards to /api/v1/media/{id}/confirm on image_backend, returns 202 on success.</done>
</task>

<task type="auto">
  <name>Task 2: Create BFF external import route at /api/media/external</name>
  <files>frontend/admin/src/app/api/media/external/route.js</files>
  <read_first>
    - frontend/admin/src/lib/image-api-client.js
    - frontend/admin/src/lib/auth.js
    - frontend/admin/src/app/api/catalog/products/[productId]/media/external/route.js
  </read_first>
  <action>
Create file `frontend/admin/src/app/api/media/external/route.js` with a single POST handler:

1. Import `NextResponse` from `next/server`, `imageBackendFetch` from `@/lib/image-api-client`, `getAccessToken` from `@/lib/auth`.

2. Export `async function POST(request)` that:
   a. Calls `const token = await getAccessToken()` — if null, return `NextResponse.json({ error: { code: 'UNAUTHORIZED' } }, { status: 401 })`.
   b. Reads the request body: `const body = await request.json()`. The expected body is `{ url: "https://..." }`.
   c. Calls `const { ok, status, data } = await imageBackendFetch('/api/v1/media/external', { method: 'POST', body: JSON.stringify(body) })`.
   d. Returns `NextResponse.json(data ?? { error: { code: 'SERVICE_UNAVAILABLE' } }, { status: ok ? 201 : (status || 502) })`.

Note: No `params` destructuring needed — this route has no dynamic segments.
The JSON body is passed through as-is from the client to image_backend.
The getAccessToken check verifies the user is logged in but the token is NOT sent to image_backend.

This follows the exact same pattern as the existing broken route at `src/app/api/catalog/products/[productId]/media/external/route.js`, but replaces `backendFetch` with `imageBackendFetch` and removes the productId scoping.
  </action>
  <verify>
    <automated>cd "C:/Users/Sanjar/Desktop/loyality/frontend/admin" && grep -q "export async function POST" "src/app/api/media/external/route.js" && grep -q "imageBackendFetch" "src/app/api/media/external/route.js" && grep -q "getAccessToken" "src/app/api/media/external/route.js" && grep -q "request.json" "src/app/api/media/external/route.js" && grep -q "api/v1/media/external" "src/app/api/media/external/route.js" && echo "OK: all patterns found"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at frontend/admin/src/app/api/media/external/route.js
    - File imports imageBackendFetch from @/lib/image-api-client (grep: "imageBackendFetch.*from.*@/lib/image-api-client")
    - File imports getAccessToken from @/lib/auth (grep: "getAccessToken.*from.*@/lib/auth")
    - File does NOT import backendFetch (grep -v: "backendFetch")
    - File reads request body via request.json() (grep: "request.json")
    - File forwards body to /api/v1/media/external (grep: "api/v1/media/external")
    - File returns 201 on success (grep: "201")
    - File returns 401 if no token (grep: "401")
  </acceptance_criteria>
  <done>POST /api/media/external route handler exists, imports imageBackendFetch (not backendFetch), checks session via getAccessToken, reads JSON body from client, forwards to /api/v1/media/external on image_backend, returns 201 on success.</done>
</task>

</tasks>

<verification>
Both new routes verified by:
1. File existence check for both route.js files
2. Import verification: imageBackendFetch (not backendFetch) in both files
3. Auth pattern: getAccessToken session check in both files
4. Correct upstream paths: /api/v1/media/{id}/confirm and /api/v1/media/external
5. Correct success status codes: 202 for confirm, 201 for external
6. Next.js build check: `cd frontend/admin && npx next build` passes (no import/syntax errors)
</verification>

<success_criteria>
- Two new route handler files exist under frontend/admin/src/app/api/media/
- Both use imageBackendFetch() with X-API-Key auth (not backendFetch with JWT)
- Confirm route forwards POST to /api/v1/media/{id}/confirm, returns 202
- External route forwards POST with JSON body to /api/v1/media/external, returns 201
- Both check getAccessToken for session validation before proxying
- Admin frontend builds without errors
</success_criteria>

<output>
After completion, create `.planning/workstreams/milestone/phases/05-bff-confirm-external-routes/05-01-SUMMARY.md`
</output>
