---
phase: 02-frontend-i18n-spec-alignment
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - product-creation-flow.md
autonomous: true
requirements: [I18N-02]

must_haves:
  truths:
    - "All i18n field references in the spec use uppercase N (I18N), matching actual backend to_camel output"
    - "The spec correctly explains that Pydantic uses .title() not .capitalize()"
    - "The spec validation table includes countryOfOrigin field added in Phase 1"
    - "The spec JSON request example for product creation includes countryOfOrigin as an optional field"
    - "The spec correctly states descriptionI18N is truly optional (I18nDict | None = None)"
  artifacts:
    - path: "product-creation-flow.md"
      provides: "Accurate product creation spec matching current backend behavior"
      contains: "titleI18N"
  key_links:
    - from: "product-creation-flow.md"
      to: "backend/src/modules/catalog/presentation/schemas.py"
      via: "Documents the same field names"
      pattern: "I18N"
---

<objective>
Update product-creation-flow.md spec to match actual backend behavior: fix all i18n naming across 19 lines (~24 individual occurrences), correct the wrong to_camel explanation, and add Phase 1 changes (countryOfOrigin, truly optional descriptionI18N).

Purpose: The spec is the integration contract between frontend and backend. Incorrect field names and wrong technical explanations cause developers to write buggy code (as demonstrated by the Bug Class 1 in Phase 2 research — the wrong spec explanation directly caused the conditional en inclusion pattern).

Output: An accurate spec file where all field names, JSON examples, and technical explanations match the actual backend behavior.
</objective>

<execution_context>
@C:/Users/Sanjar/Desktop/loyality/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Sanjar/Desktop/loyality/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/milestone/ROADMAP.md
@.planning/workstreams/milestone/REQUIREMENTS.md
@.planning/workstreams/milestone/STATE.md
@.planning/workstreams/milestone/phases/02-frontend-i18n-spec-alignment/02-RESEARCH.md
@.planning/workstreams/milestone/phases/01-backend-schema-fixes/01-01-SUMMARY.md

<interfaces>
<!-- Verified backend field names that spec must match -->

From backend/src/modules/catalog/presentation/schemas.py:
- ProductCreateRequest fields (camelCase aliases from to_camel):
  - titleI18N (required, I18nDict)
  - descriptionI18N (optional, I18nDict | None = None)
  - slug, brandId, primaryCategoryId, supplierId, sourceUrl, tags
  - countryOfOrigin (str | None, pattern=^[A-Z]{2}$, min_length=2, max_length=2) — ADDED IN PHASE 1

From Pydantic to_camel verified results:
  - to_camel("name_i18n") = "nameI18N"
  - to_camel("title_i18n") = "titleI18N"
  - to_camel("description_i18n") = "descriptionI18N"
  - to_camel("value_i18n") = "valueI18N"
  - to_camel("group_name_i18n") = "groupNameI18N"

Algorithm: to_pascal calls .title() (NOT .capitalize()). .title() capitalizes every letter after a non-alpha character (including digits). So i18n becomes I18N because n follows digit 8.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix all I18n naming, to_camel explanation, and add Phase 1 changes in spec</name>
  <files>product-creation-flow.md</files>
  <read_first>
    - product-creation-flow.md (full file — 921 lines. Read entirely to identify all 19 lines with I18n occurrences (~24 individual occurrences), the to_camel explanation at lines 36-37, the request JSON example at lines 158-172, and the validation table at lines 177-186. Also scan for storefront JSON examples with single-locale i18n dicts.)
    - backend/src/modules/catalog/presentation/schemas.py (lines 717-750 for ProductCreateRequest with countryOfOrigin to verify exact constraints)
  </read_first>
  <action>
**Category 1: Replace all `I18n` (lowercase n) with `I18N` (uppercase N) per D-07**

The spec has 19 lines containing `I18n`, with ~24 individual occurrences total (some lines have multiple). Find and replace every occurrence. The exact line numbers and replacements (from research):

| Line | Current | Replace With |
|------|---------|-------------|
| 36 | `nameI18n`, `titleI18n`, `descriptionI18n`, `valueI18n` (4 occurrences) | `nameI18N`, `titleI18N`, `descriptionI18N`, `valueI18N` |
| 93 | `groupNameI18n` (1 occurrence) | `groupNameI18N` |
| 100 | `nameI18n` (1 occurrence) | `nameI18N` |
| 102 | `descriptionI18n` (1 occurrence) | `descriptionI18N` |
| 113 | `valueI18n` (2 occurrences) | `valueI18N` |
| 160 | `titleI18n` (1 occurrence) | `titleI18N` |
| 167 | `descriptionI18n` (1 occurrence) | `descriptionI18N` |
| 179 | `titleI18n` (2 occurrences) | `titleI18N` |
| 183 | `descriptionI18n` (2 occurrences), `titleI18n` (1 occurrence) — 3 total | `descriptionI18N`, `titleI18N` |
| 206 | `titleI18n` (1 occurrence) | `titleI18N` |
| 245 | `titleI18n` (1 occurrence) | `titleI18N` |
| 624 | `nameI18n` (1 occurrence) | `nameI18N` |
| 625 | `descriptionI18n` (1 occurrence) | `descriptionI18N` |
| 632 | `descriptionI18n` (1 occurrence) | `descriptionI18N` |
| 714 | `nameI18n` (1 occurrence) | `nameI18N` |
| 717 | `nameI18n` (1 occurrence) | `nameI18N` |
| 854 | `titleI18n` (1 occurrence) | `titleI18N` |

**Total: 19 lines, ~24 individual occurrences confirmed by research.**

Use a global find-and-replace: `I18n` -> `I18N` (case-sensitive). This covers all occurrences. Verify that no `I18n` remains afterward (the 1 existing `I18N` reference at line 37 in the old wrong explanation will be rewritten in Category 2).

**Category 2: Fix the incorrect to_camel explanation at lines 36-37 per D-08**

Replace lines 36-37:
```
> **Важно**: Все i18n-поля передаются со строчной `n` в конце: `nameI18n`, `titleI18n`, `descriptionI18n`, `valueI18n`
> Pydantic `to_camel("name_i18n")` = `nameI18n` (`.capitalize()` делает первую букву заглавной, остальные — строчными, поэтому `i18n` → `I18n`, а не `I18N`)
```

With (after I18n->I18N replacement has already been applied):
```
> **Важно**: Все i18n-поля передаются с заглавной `N` в конце: `nameI18N`, `titleI18N`, `descriptionI18N`, `valueI18N`
> Pydantic `to_camel("name_i18n")` = `nameI18N`. Внутри `to_pascal` вызывает `.title()`, который делает заглавной каждую букву после не-алфавитного символа (включая цифры). Для `i18n`: `i` → `I` (после `_`), `n` → `N` (после цифры `8`). Затем `to_camel` переводит первую букву в нижний регистр.
```

**Category 3: Add countryOfOrigin to the product creation JSON example (per Phase 1 changes)**

In the JSON request example (around line 158-172), add `countryOfOrigin` as an optional field. Insert after `tags`:
```json
  "tags": ["nike", "air-max", "sneakers", "running"],
  "countryOfOrigin": "CN"
```

Note: Add trailing comma to `tags` line and add `countryOfOrigin` on the next line before the closing `}`.

**Category 4: Add countryOfOrigin to the validation table (around line 177-186)**

Add a new row to the validation table after `tags`:
```
| `countryOfOrigin`   | Опционально, `^[A-Z]{2}$`, ровно 2 символа, ISO 3166-1 alpha-2 (например: `CN`, `US`, `RU`)          |
```

**Category 5: Update descriptionI18N note in validation table (line 183)**

The existing row says "Опционально" which is correct, but update to clarify the Phase 1 fix. Replace the descriptionI18N row:
```
| `descriptionI18N`   | Опционально — **поле можно не передавать** (`null` или отсутствие). Если передаёшь, оба ключа `ru` + `en` обязательны (как у `titleI18N`) |
```

**Category 6: Add `en` locale to storefront JSON examples that only show `ru` (per D-09)**

The following specific storefront JSON example lines need `en` locale additions:

**Line 93** — `groupNameI18N` in storefront attributes response:
```json
// BEFORE:
"groupNameI18N": {"ru": "Размеры"}

// AFTER:
"groupNameI18N": {"ru": "Размеры", "en": "Sizes"}
```

**Line 102** — `descriptionI18N` in storefront attributes response:
```json
// BEFORE:
"descriptionI18N": {"ru": "Описание бренда"}

// AFTER:
"descriptionI18N": {"ru": "Описание бренда", "en": "Brand description"}
```

**Lines 113-114** — `valueI18N` entries in storefront attribute values:
```json
// BEFORE (line 113):
"valueI18N": {"ru": "Красный"}

// AFTER (line 113):
"valueI18N": {"ru": "Красный", "en": "Red"}

// BEFORE (line 114):
"valueI18N": {"ru": "Синий"}

// AFTER (line 114):
"valueI18N": {"ru": "Синий", "en": "Blue"}
```

Also scan for any other i18n dicts in JSON examples that only show a single locale and add `en` with a reasonable English translation. The `nameI18N` fields at lines around 93-114 should similarly include both locales if they only show `ru`.

**Final: Full scan per D-09**

After all the above changes, do a final read-through of the entire file to catch any other inaccuracies against current backend behavior. Verify no `I18n` (lowercase n) remains.
  </action>
  <verify>
    <automated>cd /c/Users/Sanjar/Desktop/loyality && echo "=== I18n count (should be 0) ===" && grep -c "I18n" product-creation-flow.md || true && echo "=== I18N count (should be >19) ===" && grep -c "I18N" product-creation-flow.md && echo "=== .capitalize removed ===" && grep -c "\.capitalize()" product-creation-flow.md || true && echo "=== .title() present ===" && grep -c "\.title()" product-creation-flow.md && echo "=== countryOfOrigin present ===" && grep -c "countryOfOrigin" product-creation-flow.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "I18n[^N]" product-creation-flow.md` returns 0 (no lowercase-n i18n references remain — note the [^N] to avoid matching I18N)
    - `grep -c "I18N" product-creation-flow.md` returns at least 20 (19 lines renamed + at least 1 in the corrected explanation)
    - `grep "\.title()" product-creation-flow.md` returns at least 1 match (corrected explanation mentions .title())
    - `grep -c "\.capitalize()" product-creation-flow.md` returns 0 (wrong explanation removed)
    - `grep "countryOfOrigin" product-creation-flow.md` returns at least 2 matches (1 in JSON example, 1 in validation table)
    - `grep "ISO 3166-1" product-creation-flow.md` returns at least 1 match (validation table describes the format)
    - `grep -c "nameI18n\b" product-creation-flow.md` returns 0 (using word boundary — all renamed to I18N)
    - The line that previously said "со строчной `n`" now says "с заглавной `N`"
    - `grep '"en":' product-creation-flow.md` matches on storefront example lines 93, 102, 113, 114 (en locale added)
  </acceptance_criteria>
  <done>
    All 19 lines (~24 individual occurrences) of I18n replaced with I18N. The to_camel explanation correctly describes .title() behavior. countryOfOrigin appears in both JSON example and validation table. descriptionI18N row clarifies true optionality. Storefront examples at lines 93, 102, 113-114 include both ru and en locales. No `I18n` (lowercase n) references remain in the spec. No `.capitalize()` references remain.
  </done>
</task>

</tasks>

<verification>
Full spec accuracy verification:

```bash
# 1. Zero I18n (lowercase n) remaining
grep -n "I18n[^N]" product-creation-flow.md
# Expected: no output

# 2. Correct explanation present
grep -A2 "Важно" product-creation-flow.md | head -3
# Expected: mentions I18N, .title(), not .capitalize()

# 3. countryOfOrigin documented
grep -n "countryOfOrigin" product-creation-flow.md
# Expected: at least 2 lines (JSON example + validation table)

# 4. No .capitalize() references
grep -n "capitalize" product-creation-flow.md
# Expected: no output

# 5. .title() explained
grep -n "\.title()" product-creation-flow.md
# Expected: at least 1 match

# 6. Storefront examples have en locale
grep -n '"en":' product-creation-flow.md | head -10
# Expected: matches on storefront example lines (93, 102, 113, 114 area)
```
</verification>

<success_criteria>
1. Zero occurrences of `I18n` (lowercase n) in the spec — all 19 lines (~24 individual occurrences) replaced with `I18N`
2. The to_camel explanation at lines 36-37 correctly describes `.title()` behavior (not `.capitalize()`)
3. `countryOfOrigin` field documented in both JSON example and validation table with ISO 3166-1 alpha-2 constraint
4. `descriptionI18N` validation row clarifies true optionality (`null` or absence)
5. Storefront JSON examples at lines 93, 102, 113-114 show both `ru` and `en` locale keys
</success_criteria>

<output>
After completion, create `.planning/workstreams/milestone/phases/02-frontend-i18n-spec-alignment/02-02-SUMMARY.md`
</output>
