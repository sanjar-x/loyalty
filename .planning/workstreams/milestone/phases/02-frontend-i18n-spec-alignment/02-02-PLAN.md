---
phase: 02-frontend-i18n-spec-alignment
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - product-creation-flow.md
autonomous: true
requirements: [I18N-02]

must_haves:
  truths:
    - "Spec uses I18N (uppercase N) in all camelCase i18n field references"
    - "Spec correctly explains how Pydantic to_camel produces uppercase N"
    - "Spec validation table includes countryOfOrigin field added in Phase 1"
    - "Storefront response JSON examples show both ru and en locales"
  artifacts:
    - path: "product-creation-flow.md"
      provides: "Accurate specification matching actual backend behavior"
      contains: "I18N"
  key_links:
    - from: "product-creation-flow.md"
      to: "backend/src/shared/schemas.py"
      via: "Documents the CamelModel alias behavior"
      pattern: "to_camel.*nameI18N"
---

<objective>
Update product-creation-flow.md specification to match actual backend behavior: fix all 19 I18n references to I18N (uppercase N), correct the wrong to_camel explanation, add Phase 1 changes (countryOfOrigin), and fix storefront response examples to show both locales.

Purpose: The spec is the canonical reference for integration work. Incorrect naming conventions mislead developers into using wrong field names. Missing Phase 1 changes make the spec stale.

Output: Fully accurate specification matching actual backend API behavior.
</objective>

<execution_context>
@C:/Users/Sanjar/Desktop/loyality/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Sanjar/Desktop/loyality/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/milestone/ROADMAP.md
@.planning/workstreams/milestone/REQUIREMENTS.md
@.planning/workstreams/milestone/STATE.md
@.planning/workstreams/milestone/phases/02-frontend-i18n-spec-alignment/02-CONTEXT.md
@.planning/workstreams/milestone/phases/02-frontend-i18n-spec-alignment/02-RESEARCH.md
@.planning/workstreams/milestone/phases/01-backend-schema-fixes/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix all I18n naming to I18N and correct to_camel explanation</name>
  <files>product-creation-flow.md</files>
  <read_first>
    - product-creation-flow.md (FULL FILE -- 921 lines, must read completely for comprehensive review per D-09)
    - .planning/workstreams/milestone/phases/02-frontend-i18n-spec-alignment/02-RESEARCH.md (sections: "Spec Inaccuracies" and "Code Examples" for exact corrections)
    - .planning/workstreams/milestone/phases/01-backend-schema-fixes/01-01-SUMMARY.md (to understand what Phase 1 changed)
  </read_first>
  <action>
Make ALL of the following changes to `product-creation-flow.md`. This is a single comprehensive pass per D-07, D-08, D-09.

**Category 1: Replace all 19 `I18n` (lowercase n) with `I18N` (uppercase N)**

Every occurrence of `I18n` in camelCase field names must become `I18N`. The exact 19 locations by line (line numbers are approximate -- match by content):

| Line | Old | New |
|------|-----|-----|
| ~36 | `nameI18n`, `titleI18n`, `descriptionI18n`, `valueI18n` | `nameI18N`, `titleI18N`, `descriptionI18N`, `valueI18N` |
| ~37 | `nameI18n` | `nameI18N` |
| ~93 | `"groupNameI18n"` | `"groupNameI18N"` |
| ~100 | `"nameI18n"` | `"nameI18N"` |
| ~102 | `"descriptionI18n"` | `"descriptionI18N"` |
| ~113 | `"valueI18n"` | `"valueI18N"` |
| ~114 | `"valueI18n"` | `"valueI18N"` |
| ~160 | `"titleI18n"` | `"titleI18N"` |
| ~167 | `"descriptionI18n"` | `"descriptionI18N"` |
| ~179 | `` `titleI18n` `` | `` `titleI18N` `` |
| ~183 | `` `descriptionI18n` `` (x2) | `` `descriptionI18N` `` and `` `titleI18N` `` |
| ~206 | `titleI18n` | `titleI18N` |
| ~245 | `` `titleI18n` `` | `` `titleI18N` `` |
| ~624 | `"nameI18n"` | `"nameI18N"` |
| ~625 | `"descriptionI18n"` | `"descriptionI18N"` |
| ~632 | `` `descriptionI18n` `` | `` `descriptionI18N` `` |
| ~714 | `"nameI18n"` | `"nameI18N"` |
| ~717 | `"nameI18n"` | `"nameI18N"` |
| ~854 | `"titleI18n"` | `"titleI18N"` |

The simplest approach: do a global find-and-replace of `I18n` with `I18N` (case-sensitive). But ONLY in camelCase contexts -- do NOT change standalone `i18n` (lowercase i) references like "i18n-поля" or "i18n dict".

**Category 2: Fix the incorrect to_camel explanation (lines 36-37)**

Replace the entire blockquote (lines 36-37) with:

```
> **Важно**: Все i18n-поля в camelCase используют заглавную `N`: `nameI18N`, `titleI18N`, `descriptionI18N`, `valueI18N`
> Pydantic `to_camel("name_i18n")` = `nameI18N`. Функция разбивает по `_`, делает первую букву каждого сегмента заглавной и склеивает. Для `i18n` сегмент становится `I18N` (первая буква — заглавная, остальные сохраняются как есть, НЕ приводятся к нижнему регистру).
```

Per D-08: the current explanation incorrectly claims `.capitalize()` lowercases the rest of the segment.

**Category 3: Add countryOfOrigin to validation table and JSON example (Phase 1 changes)**

In the Step 1 validation table (around line 177-186), add a new row after `tags`:

```
| `countryOfOrigin`   | ISO 3166-1 alpha-2 (`^[A-Z]{2}$`), опционально. Добавлено в Phase 1                                               |
```

In the Step 1 JSON request example (lines 158-172), add `countryOfOrigin` field to the example object (after `tags`):

```json
  "countryOfOrigin": "US"
```

This is optional but worth documenting since it was added in Phase 1 (BKND-02).

**Category 4: Fix storefront response JSON examples to show both locales**

Line ~93: `"groupNameI18N": { "ru": "Физические характеристики" }` -- add en locale:
```json
"groupNameI18N": { "ru": "Физические характеристики", "en": "Physical characteristics" }
```

Line ~102: `"descriptionI18N": { "ru": "EU размер" }` -- add en locale:
```json
"descriptionI18N": { "ru": "EU размер", "en": "EU size" }
```

Lines ~113-114: `"valueI18N": {"ru":"EU 42"}` and `"valueI18N": {"ru":"EU 43"}` -- add en locale:
```json
"valueI18N": {"ru":"EU 42", "en":"EU 42"}
"valueI18N": {"ru":"EU 43", "en":"EU 43"}
```

**Category 5: Full review pass**

Per D-09, do a full read-through of the entire 921-line file. Look for any other inaccuracies beyond the 4 categories above. Specific things to check:
- All JSON field names match actual Pydantic CamelModel output
- Status enum values match `ProductStatus` StrEnum: `draft`, `enriching`, `ready_for_review`, `published`, `archived`
- Endpoint URLs match actual router prefixes
- Response schemas match actual backend behavior
- Note about `descriptionI18N` being truly optional (Phase 1 fix) is already correct in the validation table -- verify this.
  </action>
  <verify>
    <automated>grep -c "I18n" product-creation-flow.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "I18n" product-creation-flow.md` returns 0 (zero occurrences of I18n with lowercase n in camelCase context)
    - `grep -c "I18N" product-creation-flow.md` returns at least 19 (all replaced plus any new ones)
    - product-creation-flow.md line ~36 contains `nameI18N`, `titleI18N`, `descriptionI18N`, `valueI18N` (all uppercase N)
    - product-creation-flow.md line ~37 contains "первая буква — заглавная, остальные сохраняются как есть" (correct explanation, not the old .capitalize() claim)
    - product-creation-flow.md does NOT contain `.capitalize()` in the to_camel explanation
    - product-creation-flow.md validation table contains `countryOfOrigin` row with `^[A-Z]{2}$`
    - product-creation-flow.md Step 1 JSON example contains `"countryOfOrigin"`
    - product-creation-flow.md storefront response JSON at line ~93 contains `"en":` locale in groupNameI18N
    - product-creation-flow.md storefront response JSON at line ~102 contains `"en":` locale in descriptionI18N
  </acceptance_criteria>
  <done>Spec uses I18N (uppercase N) everywhere. to_camel explanation is factually correct. countryOfOrigin documented in validation table and JSON example. Storefront response examples show both ru and en locales. Full review pass completed per D-09.</done>
</task>

</tasks>

<verification>
1. `grep -c "I18n" product-creation-flow.md` returns 0 (all 19 replaced)
2. `grep -c "I18N" product-creation-flow.md` returns >= 19 (at least the original count, possibly more)
3. `grep "countryOfOrigin" product-creation-flow.md` matches at least 2 lines (JSON example + validation table)
4. `grep ".capitalize()" product-creation-flow.md` returns nothing (wrong explanation removed)
5. `grep '"en"' product-creation-flow.md | head -5` shows en locales in early JSON examples
</verification>

<success_criteria>
- Zero occurrences of `I18n` (lowercase n in camelCase context) remain in the spec
- The to_camel explanation accurately describes Pydantic's actual behavior (uppercase N preserved)
- Phase 1 changes (countryOfOrigin, truly optional descriptionI18N) reflected in the spec
- All JSON examples show both ru and en locales for i18n fields
- Full review pass completed -- no other inaccuracies found or all found issues fixed
</success_criteria>

<output>
After completion, create `.planning/workstreams/milestone/phases/02-frontend-i18n-spec-alignment/02-02-SUMMARY.md`
</output>
