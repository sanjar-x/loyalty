---
phase: 02-value-objects-entity-foundations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/unit/modules/catalog/domain/test_brand.py
  - backend/tests/unit/modules/catalog/domain/test_category.py
  - backend/tests/unit/modules/catalog/domain/test_value_objects.py
autonomous: true
requirements: [DOM-01, DOM-05]
must_haves:
  truths:
    - "Brand.create() rejects empty name, blank name, invalid slug, empty slug"
    - "Brand.update() mutates allowed fields, rejects invalid values, respects Ellipsis sentinel for nullable fields"
    - "Brand.__setattr__ guard prevents direct slug mutation"
    - "Brand.validate_deletable() raises BrandHasProductsError when has_products=True"
    - "Category.create_root() and create_child() validate i18n, slug, sort_order, max depth, and template inheritance"
    - "Category.update() changes fields, returns old_full_slug on slug change, recomputes effective_template_id"
    - "Category.validate_deletable() raises on children or products"
    - "Money rejects negative amounts, validates currency length, uppercases currency, is frozen immutable"
    - "Money comparison operators enforce same-currency constraint"
    - "Money.from_primitives() validates compare_at > price"
    - "BehaviorFlags validates search_weight range 1-10 and is frozen immutable"
    - "validate_i18n_completeness raises MissingRequiredLocalesError when required locales missing"
    - "validate_validation_rules rejects invalid keys per data type"
    - "SLUG_RE matches valid slugs and rejects invalid patterns"
    - "All domain StrEnums have correct string values"
  artifacts:
    - path: "backend/tests/unit/modules/catalog/domain/test_brand.py"
      provides: "Unit tests for Brand create, update, guard, deletion"
      min_lines: 80
    - path: "backend/tests/unit/modules/catalog/domain/test_category.py"
      provides: "Unit tests for Category create_root, create_child, update, guard, deletion"
      min_lines: 120
    - path: "backend/tests/unit/modules/catalog/domain/test_value_objects.py"
      provides: "Unit tests for Money, BehaviorFlags, enums, i18n validation, slug regex, validation_rules"
      min_lines: 150
  key_links:
    - from: "backend/tests/unit/modules/catalog/domain/test_brand.py"
      to: "backend/src/modules/catalog/domain/entities.py"
      via: "imports Brand, uses BrandBuilder"
      pattern: "from src.modules.catalog.domain.entities import Brand"
    - from: "backend/tests/unit/modules/catalog/domain/test_category.py"
      to: "backend/src/modules/catalog/domain/entities.py"
      via: "imports Category"
      pattern: "from src.modules.catalog.domain.entities import Category"
    - from: "backend/tests/unit/modules/catalog/domain/test_value_objects.py"
      to: "backend/src/modules/catalog/domain/value_objects.py"
      via: "imports Money, BehaviorFlags, enums, validation functions"
      pattern: "from src.modules.catalog.domain.value_objects import"
---

<objective>
Unit tests for Brand entity, Category entity, and all value objects (Money, BehaviorFlags, enums, validation helpers).

Purpose: Prove that Brand and Category factory methods, update methods, guarded fields, and deletion guards work correctly. Prove all value objects are immutable, validated, and have correct behavior. This covers the simpler standalone entities and the complete DOM-05 requirement.

Output: Three test files with passing tests covering Brand lifecycle, Category tree operations, and all value objects.
</objective>

<execution_context>
@C:/Users/Sanjar/Desktop/loyality/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Sanjar/Desktop/loyality/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-value-objects-entity-foundations/02-CONTEXT.md
@.planning/phases/02-value-objects-entity-foundations/02-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From backend/src/modules/catalog/domain/entities.py (Brand, lines 163-266):
- Brand.create(name: str, slug: str, logo_url: str | None = None, logo_storage_object_id: uuid.UUID | None = None, brand_id: uuid.UUID | None = None) -> Brand
- Brand.update(name=..., slug=..., logo_url=..., logo_storage_object_id=...) — Ellipsis sentinel for "keep current", None for "clear"
- Brand.__setattr__ guard: _BRAND_GUARDED_FIELDS = frozenset({"slug"})
- Brand.validate_deletable(has_products: bool) -> None — raises BrandHasProductsError

From backend/src/modules/catalog/domain/entities.py (Category, lines 277-491):
- Category.create_root(name_i18n, slug, template_id=None, sort_order=0, category_id=None) -> Category
- Category.create_child(name_i18n, slug, parent: Category, template_id=None, sort_order=0, category_id=None) -> Category
  - Enforces MAX_CATEGORY_DEPTH = 3
  - full_slug = f"{parent.full_slug}/{slug}"
  - effective_template_id = template_id or parent.effective_template_id
- Category._UPDATABLE_FIELDS = {"name_i18n", "slug", "sort_order", "template_id", "description_i18n"}
- Category.update(**kwargs) -> str | None — returns old_full_slug when slug changes
- Category.validate_deletable(has_children: bool, has_products: bool) -> None
- Category.set_effective_template_id(value: uuid.UUID | None) -> None
- _CATEGORY_GUARDED_FIELDS = frozenset({"slug"})

From backend/src/modules/catalog/domain/value_objects.py:
- Money(@frozen): amount: int, currency: str — validates non-negative, 3-char currency, uppercases
- Money.from_primitives(amount, currency, compare_at_amount=None) -> tuple[Money, Money | None]
- Money.__lt__, __le__, __gt__, __ge__ — enforce same-currency via _check_currency
- BehaviorFlags(@frozen): is_filterable, is_searchable, search_weight (1-10), is_comparable, is_visible_on_card
- validate_i18n_completeness(i18n_dict, field_name) — raises MissingRequiredLocalesError
- validate_validation_rules(data_type, rules) — validates keys per data type
- SLUG_RE = re.compile(r"^[a-z0-9]+(?:-[a-z0-9]+)*$")
- Enums: MediaType, MediaRole, AttributeDataType, AttributeUIType, AttributeLevel, RequirementLevel, ProductStatus

From backend/src/modules/catalog/domain/exceptions.py:
- BrandHasProductsError
- CategoryHasChildrenError, CategoryHasProductsError, CategoryMaxDepthError
- MissingRequiredLocalesError

From backend/tests/factories/brand_builder.py:
- BrandBuilder().with_name("X").with_slug("x").with_logo(url, obj_id).build() -> Brand

From backend/src/modules/catalog/application/constants.py:
- REQUIRED_LOCALES = frozenset({"ru", "en"})
- DEFAULT_SEARCH_WEIGHT = 5
- DEFAULT_CURRENCY = "RUB"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Brand entity tests and value objects tests</name>
  <files>
    backend/tests/unit/modules/catalog/domain/test_brand.py
    backend/tests/unit/modules/catalog/domain/test_value_objects.py
  </files>
  <read_first>
    backend/src/modules/catalog/domain/entities.py (lines 163-266 for Brand)
    backend/src/modules/catalog/domain/value_objects.py
    backend/src/modules/catalog/domain/exceptions.py
    backend/tests/factories/brand_builder.py
    backend/tests/unit/modules/catalog/domain/test_category_effective_family.py (existing pattern)
  </read_first>
  <action>
Create `backend/tests/unit/modules/catalog/domain/test_brand.py` with these test classes per D-07:

**TestBrand** (factory method tests per D-03):
- `test_create_with_valid_inputs` — BrandBuilder().with_name("Nike").with_slug("nike").build(), assert name=="Nike", slug=="nike", isinstance(id, uuid.UUID)
- `test_create_strips_name_whitespace` — Brand.create(name="  Nike  ", slug="nike"), assert name=="Nike"
- `test_create_with_logo` — BrandBuilder().with_logo("https://img.co/logo.png", uuid.uuid4()).build(), assert logo_url and logo_storage_object_id set
- `test_create_without_logo` — BrandBuilder().build(), assert logo_url is None
- `test_create_rejects_empty_name` — pytest.raises(ValueError, match="Brand name must be non-empty"), Brand.create(name="", slug="valid")
- `test_create_rejects_blank_name` — pytest.raises(ValueError, match="Brand name must be non-empty"), Brand.create(name="   ", slug="valid")
- `test_create_rejects_invalid_slug` — pytest.raises(ValueError, match="slug must be non-empty"), Brand.create(name="Nike", slug="Bad Slug!")
- `test_create_rejects_empty_slug` — pytest.raises(ValueError, match="slug must be non-empty"), Brand.create(name="Nike", slug="")

**TestBrandUpdate** (update method tests):
- `test_update_name` — brand.update(name="Updated"), assert name=="Updated"
- `test_update_slug_via_update_method` — brand.update(slug="new-slug"), assert slug=="new-slug"
- `test_update_rejects_empty_name` — pytest.raises(ValueError), brand.update(name="")
- `test_update_logo_url_to_none_clears_it` — brand with logo, brand.update(logo_url=None), assert logo_url is None
- `test_update_logo_url_omitted_keeps_current` — brand with logo, brand.update(name="New"), assert logo_url unchanged (Ellipsis sentinel test per Pitfall 2)

**TestBrandGuard** (DDD-01 guard):
- `test_direct_slug_assignment_raises` — pytest.raises(AttributeError, match="Cannot set 'slug' directly")

**TestBrandDeletion** (validate_deletable):
- `test_deletable_when_no_products` — brand.validate_deletable(has_products=False), no exception
- `test_not_deletable_when_has_products` — pytest.raises(BrandHasProductsError), brand.validate_deletable(has_products=True)

Create `backend/tests/unit/modules/catalog/domain/test_value_objects.py` with these test classes per D-07:

**TestMoney:**
- `test_create_valid` — Money(amount=1000, currency="RUB"), assert amount==1000, currency=="RUB"
- `test_currency_uppercased` — Money(amount=100, currency="rub"), assert currency=="RUB"
- `test_rejects_negative_amount` — pytest.raises(ValueError, match="non-negative")
- `test_zero_amount_allowed` — Money(amount=0, currency="RUB"), assert amount==0
- `test_rejects_invalid_currency_length` — pytest.raises(ValueError, match="3-character"), Money(amount=100, currency="US")
- `test_rejects_long_currency` — pytest.raises(ValueError, match="3-character"), Money(amount=100, currency="USDX")
- `test_frozen_rejects_mutation` — from attrs.exceptions import FrozenInstanceError, pytest.raises(FrozenInstanceError), m.amount = 2000
- `test_equality` — Money(100, "RUB") == Money(100, "RUB")
- `test_inequality_different_amount` — Money(100, "RUB") != Money(200, "RUB")
- `test_lt_same_currency` — Money(100, "RUB") < Money(200, "RUB") is True
- `test_le_same_currency` — Money(100, "RUB") <= Money(100, "RUB") is True
- `test_gt_same_currency` — Money(200, "RUB") > Money(100, "RUB") is True
- `test_ge_same_currency` — Money(200, "RUB") >= Money(200, "RUB") is True
- `test_comparison_different_currency_raises` — pytest.raises(ValueError, match="different currencies"), Money(100, "RUB") < Money(200, "USD")
- `test_from_primitives_with_compare_at` — Money.from_primitives(1000, "RUB", compare_at_amount=2000), assert price.amount==1000, compare.amount==2000
- `test_from_primitives_without_compare_at` — Money.from_primitives(1000, "RUB"), assert compare is None
- `test_from_primitives_rejects_compare_at_not_greater` — pytest.raises(ValueError, match="compare_at_price must be greater"), Money.from_primitives(1000, "RUB", compare_at_amount=500)
- `test_from_primitives_rejects_compare_at_equal` — pytest.raises(ValueError, match="compare_at_price must be greater"), Money.from_primitives(1000, "RUB", compare_at_amount=1000)

**TestBehaviorFlags:**
- `test_defaults` — BehaviorFlags(), assert is_filterable==False, is_searchable==False, search_weight==5, is_comparable==False, is_visible_on_card==False
- `test_custom_values` — BehaviorFlags(is_filterable=True, search_weight=8)
- `test_frozen_rejects_mutation` — FrozenInstanceError
- `test_search_weight_below_min_raises` — pytest.raises(ValueError, match="search_weight must be between"), BehaviorFlags(search_weight=0)
- `test_search_weight_above_max_raises` — pytest.raises(ValueError, match="search_weight must be between"), BehaviorFlags(search_weight=11)
- `test_search_weight_at_boundaries` — BehaviorFlags(search_weight=1), BehaviorFlags(search_weight=10), both valid

**TestValidateI18nCompleteness:**
- `test_valid_both_locales` — validate_i18n_completeness({"en": "Name", "ru": "Имя"}) passes
- `test_missing_ru_raises` — pytest.raises(MissingRequiredLocalesError), validate_i18n_completeness({"en": "Name"})
- `test_missing_en_raises` — pytest.raises(MissingRequiredLocalesError)
- `test_empty_dict_raises` — pytest.raises(MissingRequiredLocalesError)

**TestValidateValidationRules:**
- `test_none_rules_passes` — validate_validation_rules(AttributeDataType.STRING, None)
- `test_empty_dict_passes` — validate_validation_rules(AttributeDataType.STRING, {})
- `test_string_valid_keys` — validate_validation_rules(AttributeDataType.STRING, {"min_length": 1, "max_length": 100})
- `test_string_invalid_keys_raises` — pytest.raises(ValueError), validate_validation_rules(AttributeDataType.STRING, {"min_value": 1})
- `test_numeric_valid_keys` — validate_validation_rules(AttributeDataType.INTEGER, {"min_value": 0, "max_value": 100})
- `test_numeric_invalid_keys_raises` — pytest.raises(ValueError), validate_validation_rules(AttributeDataType.INTEGER, {"min_length": 1})
- `test_boolean_rejects_all_keys` — pytest.raises(ValueError), validate_validation_rules(AttributeDataType.BOOLEAN, {"min_value": 0})
- `test_string_min_exceeds_max_raises` — pytest.raises(ValueError, match="min_length cannot exceed max_length")
- `test_numeric_min_exceeds_max_raises` — pytest.raises(ValueError, match="min_value cannot exceed max_value")

**TestSlugRegex:**
- `test_valid_simple_slug` — SLUG_RE.match("nike") is truthy
- `test_valid_slug_with_hyphens` — SLUG_RE.match("nike-air-max") is truthy
- `test_valid_slug_with_digits` — SLUG_RE.match("product-123") is truthy
- `test_rejects_uppercase` — SLUG_RE.match("Nike") is None
- `test_rejects_spaces` — SLUG_RE.match("nike air") is None
- `test_rejects_leading_hyphen` — SLUG_RE.match("-nike") is None
- `test_rejects_trailing_hyphen` — SLUG_RE.match("nike-") is None
- `test_rejects_empty` — SLUG_RE.match("") is None
- `test_rejects_consecutive_hyphens` — SLUG_RE.match("nike--air") is None

**TestEnums:**
- `test_product_status_values` — assert ProductStatus.DRAFT == "draft", ENRICHING == "enriching", etc. for all 5 values
- `test_media_type_values` — assert all 4 values match expected strings
- `test_media_role_values` — assert all 6 values
- `test_attribute_data_type_values` — assert all 4 values
- `test_attribute_ui_type_values` — assert all 5 values
- `test_attribute_level_values` — assert PRODUCT == "product", VARIANT == "variant"
- `test_requirement_level_values` — assert REQUIRED, RECOMMENDED, OPTIONAL

All tests are plain `def test_...` (no async) per D-08. Use builders from Phase 1 per D-06.
  </action>
  <verify>
    <automated>cd C:/Users/Sanjar/Desktop/loyality/backend && uv run pytest tests/unit/modules/catalog/domain/test_brand.py tests/unit/modules/catalog/domain/test_value_objects.py -x -q --no-cov --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `test_brand.py` has classes TestBrand, TestBrandUpdate, TestBrandGuard, TestBrandDeletion
    - `test_value_objects.py` has classes TestMoney, TestBehaviorFlags, TestValidateI18nCompleteness, TestValidateValidationRules, TestSlugRegex, TestEnums
    - All tests pass with exit code 0
    - No async test functions (plain `def test_...` only)
    - grep "class TestBrand" test_brand.py returns match
    - grep "class TestMoney" test_value_objects.py returns match
    - grep "BrandBuilder" test_brand.py returns match (using Phase 1 builders per D-06)
  </acceptance_criteria>
  <done>
    Brand entity tests cover create (valid + 4 rejection cases), update (5 cases including Ellipsis sentinel), guard (1 case), deletion (2 cases). Value object tests cover Money (18 cases), BehaviorFlags (6 cases), i18n validation (4 cases), validation rules (9 cases), slug regex (9 cases), enums (7 cases). All tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Category entity tests</name>
  <files>
    backend/tests/unit/modules/catalog/domain/test_category.py
  </files>
  <read_first>
    backend/src/modules/catalog/domain/entities.py (lines 277-491 for Category)
    backend/src/modules/catalog/domain/exceptions.py
    backend/tests/unit/modules/catalog/domain/test_category_effective_family.py (existing pattern to follow)
  </read_first>
  <action>
Create `backend/tests/unit/modules/catalog/domain/test_category.py` with these test classes per D-07.

Include the `_i18n` helper at top of file (same pattern as existing test_category_effective_family.py):
```python
def _i18n(en: str, ru: str | None = None) -> dict[str, str]:
    return {"en": en, "ru": ru or en}
```

**TestCategoryCreateRoot** (factory method tests per D-03):
- `test_create_root_valid` — Category.create_root(name_i18n=_i18n("Electronics"), slug="electronics"), assert name_i18n has en and ru, slug=="electronics", parent_id is None, level==0, full_slug=="electronics"
- `test_create_root_with_template` — pass template_id=uuid.uuid4(), assert template_id and effective_template_id set
- `test_create_root_without_template` — assert template_id is None, effective_template_id is None
- `test_create_root_custom_sort_order` — sort_order=5, assert sort_order==5
- `test_create_root_rejects_empty_slug` — pytest.raises(ValueError, match="slug must be non-empty")
- `test_create_root_rejects_invalid_slug` — pytest.raises(ValueError, match="slug must be non-empty"), slug="Bad Slug!"
- `test_create_root_rejects_missing_locale` — pytest.raises(MissingRequiredLocalesError), name_i18n={"en": "Only English"}
- `test_create_root_rejects_blank_i18n_values` — pytest.raises(ValueError, match="must not contain empty or blank"), name_i18n={"en": "", "ru": "Valid"}
- `test_create_root_rejects_negative_sort_order` — pytest.raises(ValueError, match="sort_order must be non-negative"), sort_order=-1

**TestCategoryCreateChild:**
- `test_create_child_valid` — create parent root, then Category.create_child(name_i18n=_i18n("Phones"), slug="phones", parent=parent), assert level==1, parent_id==parent.id, full_slug=="electronics/phones"
- `test_create_child_inherits_template` — parent with template_id, child without, assert child.effective_template_id == parent template
- `test_create_child_overrides_template` — parent and child both have template_id, assert child.effective_template_id == child's template
- `test_create_child_max_depth_enforcement` — MAX_CATEGORY_DEPTH = 3, create chain root->child->grandchild (level 2), then pytest.raises(CategoryMaxDepthError) on 4th level
- `test_create_child_level_2_allowed` — create root (0) -> child (1) -> grandchild (2), assert grandchild.level == 2
- `test_create_child_rejects_invalid_slug` — pytest.raises(ValueError), slug="Bad Slug!"

**TestCategoryUpdate:**
- `test_update_name_i18n` — cat.update(name_i18n=_i18n("New Name")), assert name_i18n updated
- `test_update_slug_returns_old_full_slug` — cat with slug "old", result = cat.update(slug="new"), assert result == "old" (old full_slug), assert cat.slug == "new"
- `test_update_without_slug_returns_none` — result = cat.update(name_i18n=_i18n("X")), assert result is None
- `test_update_sort_order` — cat.update(sort_order=10), assert sort_order==10
- `test_update_template_id_recomputes_effective` — cat.update(template_id=new_id), assert effective_template_id == new_id
- `test_update_template_id_to_none` — cat with template, cat.update(template_id=None), assert template_id is None (but effective NOT auto-cleared per existing test)
- `test_update_rejects_unknown_field` — pytest.raises(TypeError), cat.update(unknown_field="x")
- `test_update_rejects_invalid_slug` — pytest.raises(ValueError), cat.update(slug="Bad Slug!")
- `test_update_rejects_negative_sort_order` — pytest.raises(ValueError), cat.update(sort_order=-1)

**TestCategoryGuard:**
- `test_direct_slug_assignment_raises` — pytest.raises(AttributeError, match="Cannot set 'slug' directly")

**TestCategoryDeletion:**
- `test_deletable_when_no_children_no_products` — cat.validate_deletable(has_children=False, has_products=False), no exception
- `test_not_deletable_when_has_children` — pytest.raises(CategoryHasChildrenError)
- `test_not_deletable_when_has_products` — pytest.raises(CategoryHasProductsError)

**TestCategorySetEffectiveTemplateId:**
- `test_set_to_uuid` — cat.set_effective_template_id(uuid.uuid4()), assert updated
- `test_set_to_none` — cat.set_effective_template_id(None), assert None

All tests plain `def test_...` per D-08. The existing `test_category_effective_family.py` already covers some effective_template_id behavior (9 tests) -- these tests complement it with broader create/update/guard/deletion coverage.
  </action>
  <verify>
    <automated>cd C:/Users/Sanjar/Desktop/loyality/backend && uv run pytest tests/unit/modules/catalog/domain/test_category.py -x -q --no-cov --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `test_category.py` has classes TestCategoryCreateRoot, TestCategoryCreateChild, TestCategoryUpdate, TestCategoryGuard, TestCategoryDeletion, TestCategorySetEffectiveTemplateId
    - All tests pass with exit code 0
    - grep "class TestCategoryCreateRoot" test_category.py returns match
    - grep "class TestCategoryCreateChild" test_category.py returns match
    - grep "CategoryMaxDepthError" test_category.py returns match (max depth test exists)
    - grep "def _i18n" test_category.py returns match (helper present)
    - No async test functions
  </acceptance_criteria>
  <done>
    Category entity tests cover create_root (9 cases), create_child (6 cases), update (9 cases), guard (1 case), deletion (3 cases), set_effective_template_id (2 cases). Total ~30 tests. All pass. Complements existing 9 tests in test_category_effective_family.py.
  </done>
</task>

</tasks>

<verification>
Run complete domain test suite to ensure no conflicts with existing tests:
```bash
cd C:/Users/Sanjar/Desktop/loyality/backend && uv run pytest tests/unit/modules/catalog/domain/ -x -q --no-cov --tb=short
```
Expected: All tests in test_brand.py, test_category.py, test_value_objects.py, and existing test_category_effective_family.py pass together.
</verification>

<success_criteria>
1. test_brand.py exists with >= 15 tests covering Brand create, update, guard, deletion
2. test_value_objects.py exists with >= 40 tests covering Money, BehaviorFlags, enums, i18n, slug regex, validation rules
3. test_category.py exists with >= 25 tests covering Category create_root, create_child, update, guard, deletion
4. All tests pass: `uv run pytest tests/unit/modules/catalog/domain/ -x -q --no-cov` exits 0
5. No async test functions (grep "async def test_" returns 0 matches)
6. Builders from Phase 1 used (grep "BrandBuilder" test_brand.py returns matches)
</success_criteria>

<output>
After completion, create `.planning/phases/02-value-objects-entity-foundations/02-01-SUMMARY.md`
</output>
