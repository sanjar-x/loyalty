---
phase: 02-value-objects-entity-foundations
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/unit/modules/catalog/domain/test_attribute.py
  - backend/tests/unit/modules/catalog/domain/test_attribute_template.py
  - backend/tests/unit/modules/catalog/domain/test_attribute_group.py
autonomous: true
requirements: [DOM-01]
must_haves:
  truths:
    - "Attribute.create() validates code, slug, i18n, data_type, behavior flags, validation_rules, and level"
    - "Attribute.update() mutates allowed fields, rejects unknown fields, validates search_weight range"
    - "Attribute.__setattr__ guard prevents direct code and slug mutation"
    - "Attribute behavior properties (is_filterable, is_searchable, etc.) delegate to BehaviorFlags"
    - "AttributeValue.create() validates i18n, slug, sort_order"
    - "AttributeValue.update() mutates allowed fields"
    - "ProductAttributeValue.create() generates auto ID"
    - "AttributeTemplate.create() validates i18n, sort_order"
    - "AttributeTemplate.update() mutates only _UPDATABLE_FIELDS, rejects unknown"
    - "AttributeTemplate.__setattr__ guard prevents direct code mutation"
    - "AttributeTemplate.validate_deletable() raises when has_category_refs"
    - "TemplateAttributeBinding.create() validates sort_order, filter_settings (max 20 keys, allowed key whitelist), default requirement_level"
    - "TemplateAttributeBinding.update() mutates allowed fields"
    - "AttributeGroup.create() validates i18n, sort_order"
    - "AttributeGroup.update() uses explicit kwargs (name_i18n, sort_order), not **kwargs"
    - "AttributeGroup.__setattr__ guard prevents direct code mutation"
  artifacts:
    - path: "backend/tests/unit/modules/catalog/domain/test_attribute.py"
      provides: "Unit tests for Attribute, AttributeValue, ProductAttributeValue"
      min_lines: 120
    - path: "backend/tests/unit/modules/catalog/domain/test_attribute_template.py"
      provides: "Unit tests for AttributeTemplate, TemplateAttributeBinding"
      min_lines: 80
    - path: "backend/tests/unit/modules/catalog/domain/test_attribute_group.py"
      provides: "Unit tests for AttributeGroup"
      min_lines: 50
  key_links:
    - from: "backend/tests/unit/modules/catalog/domain/test_attribute.py"
      to: "backend/src/modules/catalog/domain/entities.py"
      via: "imports Attribute, AttributeValue, ProductAttributeValue"
      pattern: "from src.modules.catalog.domain.entities import Attribute"
    - from: "backend/tests/unit/modules/catalog/domain/test_attribute_template.py"
      to: "backend/src/modules/catalog/domain/entities.py"
      via: "imports AttributeTemplate, TemplateAttributeBinding"
      pattern: "from src.modules.catalog.domain.entities import AttributeTemplate"
    - from: "backend/tests/unit/modules/catalog/domain/test_attribute_group.py"
      to: "backend/src/modules/catalog/domain/entities.py"
      via: "imports AttributeGroup"
      pattern: "from src.modules.catalog.domain.entities import AttributeGroup"
---

<objective>
Unit tests for the EAV attribute entities: Attribute (with AttributeValue and ProductAttributeValue), AttributeTemplate (with TemplateAttributeBinding), and AttributeGroup.

Purpose: Prove the EAV attribute subsystem's factory methods, update methods, guarded fields, and validation rules work correctly. This covers D-04 priority 3 (EAV attribute assignment) and completes DOM-01 coverage for all remaining entity classes.

Output: Three test files covering all attribute-related entities with passing tests.
</objective>

<execution_context>
@C:/Users/Sanjar/Desktop/loyality/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Sanjar/Desktop/loyality/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-value-objects-entity-foundations/02-CONTEXT.md
@.planning/phases/02-value-objects-entity-foundations/02-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From backend/src/modules/catalog/domain/entities.py (Attribute, lines 794-1068):
- Attribute.create(code, slug, name_i18n, data_type, ui_type, is_dictionary, group_id, description_i18n=None, level=AttributeLevel.PRODUCT, behavior=None, is_filterable=False, is_searchable=False, search_weight=DEFAULT_SEARCH_WEIGHT, is_comparable=False, is_visible_on_card=False, validation_rules=None, attribute_id=None) -> Attribute
  - Can pass behavior as BehaviorFlags object OR individual flags (is_filterable, etc.)
  - Validates code via _validate_slug, slug via _validate_slug, i18n completeness, validation_rules per data type
- Attribute._UPDATABLE_FIELDS = {"name_i18n", "ui_type", "level", "group_id", "description_i18n", "behavior", "is_filterable", "is_searchable", "search_weight", "is_comparable", "is_visible_on_card", "validation_rules"}
- Attribute.update(**kwargs) -- handles behavior as BehaviorFlags or individual flags, validates search_weight range
- _ATTRIBUTE_GUARDED_FIELDS = frozenset({"code", "slug"})
- Attribute properties: is_filterable, is_searchable, search_weight, is_comparable, is_visible_on_card (delegate to self.behavior)

From backend/src/modules/catalog/domain/entities.py (AttributeValue, lines 1072-1210):
- AttributeValue.create(attribute_id, code, slug, value_i18n, search_aliases=None, meta_data=None, value_group=None, sort_order=0, is_active=True, value_id=None) -> AttributeValue
  - Validates i18n completeness, i18n values non-blank, slug, sort_order
- AttributeValue._UPDATABLE_FIELDS = {"value_i18n", "search_aliases", "meta_data", "value_group", "sort_order", "is_active"}
- AttributeValue.update(**kwargs)

From backend/src/modules/catalog/domain/entities.py (ProductAttributeValue, lines 1213-1258):
- ProductAttributeValue.create(product_id, attribute_id, attribute_value_id, pav_id=None) -> ProductAttributeValue

From backend/src/modules/catalog/domain/entities.py (AttributeTemplate, lines 499-594):
- AttributeTemplate.create(code, name_i18n, description_i18n=None, sort_order=0, template_id=None) -> AttributeTemplate
  - Validates i18n completeness, i18n non-blank, sort_order
- AttributeTemplate._UPDATABLE_FIELDS = {"name_i18n", "description_i18n", "sort_order"}
- AttributeTemplate.update(**kwargs) -- rejects unknown fields via TypeError
- _TEMPLATE_GUARDED_FIELDS = frozenset({"code"})
- AttributeTemplate.validate_deletable(has_category_refs: bool) -> None -- raises AttributeTemplateHasCategoryReferencesError

From backend/src/modules/catalog/domain/entities.py (TemplateAttributeBinding, lines 602-688):
- TemplateAttributeBinding.create(template_id, attribute_id, sort_order=0, requirement_level=RequirementLevel.OPTIONAL, filter_settings=None, binding_id=None) -> TemplateAttributeBinding
  - Validates sort_order, filter_settings via _validate_filter_settings
- TemplateAttributeBinding._UPDATABLE_FIELDS = {"sort_order", "requirement_level", "filter_settings"}
- TemplateAttributeBinding.update(**kwargs)

From backend/src/modules/catalog/domain/entities.py (_validate_filter_settings, lines 116-137):
- Validates filter_settings: must be dict, keys must be in _FILTER_SETTINGS_ALLOWED_KEYS, max _FILTER_SETTINGS_MAX_KEYS (= 20) keys
- _FILTER_SETTINGS_ALLOWED_KEYS = frozenset({"widget", "min", "max", "step", "unit", "collapsed", "behavior", "display", "options"})
- _FILTER_SETTINGS_MAX_KEYS = 20

From backend/src/modules/catalog/domain/entities.py (AttributeGroup, lines 696-786):
- AttributeGroup.create(code, name_i18n, sort_order=0, group_id=None) -> AttributeGroup
  - Validates i18n completeness, i18n non-blank, sort_order
- AttributeGroup.update(name_i18n=..., sort_order=...) -- uses EXPLICIT kwargs (not **kwargs)
  NOTE: This is different from other entities. update() has explicit parameters, not **kwargs.
  The Ellipsis sentinel is used for the default of name_i18n parameter.
- _ATTRIBUTE_GROUP_GUARDED_FIELDS = frozenset({"code"})

From builders:
- AttributeBuilder().with_code("color").with_slug("color").with_data_type(...).build() -> Attribute
- AttributeValueBuilder().with_attribute_id(id).with_code("red").with_slug("red").build() -> AttributeValue
- ProductAttributeValueBuilder().with_product_id(id).with_attribute_id(id).build() -> ProductAttributeValue
- AttributeTemplateBuilder().with_code("clothing").build() -> AttributeTemplate
- TemplateAttributeBindingBuilder().with_template_id(id).with_attribute_id(id).build() -> TemplateAttributeBinding
- AttributeGroupBuilder().with_code("general").build() -> AttributeGroup

From backend/src/modules/catalog/domain/exceptions.py:
- AttributeTemplateHasCategoryReferencesError
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Attribute, AttributeValue, and ProductAttributeValue tests</name>
  <files>
    backend/tests/unit/modules/catalog/domain/test_attribute.py
  </files>
  <read_first>
    backend/src/modules/catalog/domain/entities.py (lines 794-1258 for Attribute, AttributeValue, ProductAttributeValue)
    backend/tests/factories/attribute_builder.py
    backend/src/modules/catalog/domain/value_objects.py (BehaviorFlags, AttributeDataType, AttributeLevel, validate_validation_rules)
  </read_first>
  <action>
Create `backend/tests/unit/modules/catalog/domain/test_attribute.py` with these test classes per D-07.

Include helper:
```python
def _i18n(en: str, ru: str | None = None) -> dict[str, str]:
    return {"en": en, "ru": ru or en}
```

**TestAttributeCreate:**
- `test_create_valid` -- AttributeBuilder().build(), assert isinstance(id, uuid.UUID), data_type==AttributeDataType.STRING, level==AttributeLevel.PRODUCT
- `test_create_with_behavior_flags_object` -- AttributeBuilder().with_behavior(BehaviorFlags(is_filterable=True, search_weight=8)).build(), assert attribute.behavior.is_filterable is True, attribute.behavior.search_weight == 8
- `test_create_with_individual_behavior_flags` -- Attribute.create(code="x", slug="x", name_i18n=_i18n("X"), data_type=AttributeDataType.STRING, ui_type=AttributeUIType.DROPDOWN, is_dictionary=True, group_id=uuid.uuid4(), is_filterable=True, search_weight=8), assert attribute.is_filterable is True
- `test_create_at_variant_level` -- AttributeBuilder().at_variant_level().build(), assert level==AttributeLevel.VARIANT
- `test_create_with_validation_rules` -- AttributeBuilder().with_data_type(AttributeDataType.STRING).with_validation_rules({"min_length": 1, "max_length": 100}).build(), assert validation_rules set
- `test_create_rejects_invalid_code` -- pytest.raises(ValueError, match="slug must be non-empty"), Attribute.create(code="BAD CODE!", slug="valid", name_i18n=_i18n("X"), data_type=AttributeDataType.STRING, ui_type=AttributeUIType.DROPDOWN, is_dictionary=True, group_id=uuid.uuid4())
  NOTE: code is validated via the same _validate_slug function as slug (uses SLUG_RE regex)
- `test_create_rejects_invalid_slug` -- pytest.raises(ValueError), slug="Bad Slug!"
- `test_create_rejects_missing_locale` -- pytest.raises(MissingRequiredLocalesError), name_i18n={"en": "Only English"} (Pitfall 5)
- `test_create_rejects_blank_i18n_values` -- pytest.raises(ValueError), name_i18n={"en": "", "ru": "Valid"}
- `test_create_rejects_invalid_validation_rules_for_type` -- pytest.raises(ValueError), data_type=STRING with rules={"min_value": 0}

**TestAttributeUpdate:**
- `test_update_name_i18n` -- attr.update(name_i18n=_i18n("New Name")), assert updated
- `test_update_ui_type` -- attr.update(ui_type=AttributeUIType.COLOR_SWATCH), assert updated
- `test_update_level` -- attr.update(level=AttributeLevel.VARIANT), assert level changed
- `test_update_group_id` -- attr.update(group_id=uuid.uuid4()), assert updated
- `test_update_group_id_to_none` -- attr.update(group_id=None), assert group_id is None
- `test_update_behavior_via_object` -- attr.update(behavior=BehaviorFlags(is_filterable=True)), assert attr.behavior.is_filterable is True
- `test_update_behavior_via_individual_flags` -- attr.update(is_filterable=True, search_weight=7), assert attr.is_filterable is True, attr.search_weight == 7
- `test_update_validation_rules` -- attr.update(validation_rules={"min_length": 5}), assert updated
- `test_update_rejects_unknown_field` -- pytest.raises(TypeError), attr.update(unknown="x")
- `test_update_rejects_search_weight_below_min` -- pytest.raises(ValueError), attr.update(search_weight=0)
- `test_update_rejects_search_weight_above_max` -- pytest.raises(ValueError), attr.update(search_weight=11)

**TestAttributeGuard** (Pitfall 2):
- `test_direct_code_assignment_raises` -- pytest.raises(AttributeError, match="Cannot set 'code' directly")
- `test_direct_slug_assignment_raises` -- pytest.raises(AttributeError, match="Cannot set 'slug' directly")

**TestAttributeProperties:**
- `test_is_filterable_delegates_to_behavior` -- attr with BehaviorFlags(is_filterable=True), assert attr.is_filterable is True
- `test_is_searchable_delegates` -- similarly
- `test_search_weight_delegates` -- attr with search_weight=8, assert attr.search_weight == 8
- `test_is_comparable_delegates` -- similarly
- `test_is_visible_on_card_delegates` -- similarly

**TestAttributeValueCreate:**
- `test_create_valid` -- AttributeValueBuilder().build(), assert isinstance(id, uuid.UUID)
- `test_create_rejects_missing_locale` -- pytest.raises(MissingRequiredLocalesError), AttributeValue.create(attribute_id=uuid.uuid4(), code="red", slug="red", value_i18n={"en": "Red"})
- `test_create_rejects_blank_i18n_values` -- pytest.raises(ValueError)
- `test_create_rejects_invalid_slug` -- pytest.raises(ValueError)
- `test_create_rejects_negative_sort_order` -- pytest.raises(ValueError, match="sort_order must be non-negative")
- `test_create_defaults` -- assert is_active==True, sort_order==0, search_aliases empty or None, meta_data empty or None
- `test_create_with_optional_fields` -- AttributeValue.create with search_aliases=["scarlet"], meta_data={"hex": "#FF0000"}, value_group="warm", assert all set

**TestAttributeValueUpdate:**
- `test_update_value_i18n` -- val.update(value_i18n=_i18n("Blue")), assert updated
- `test_update_sort_order` -- val.update(sort_order=5), assert updated
- `test_update_is_active` -- val.update(is_active=False), assert is_active is False
- `test_update_search_aliases` -- val.update(search_aliases=["crimson", "scarlet"]), assert updated
- `test_update_meta_data` -- val.update(meta_data={"hex": "#FF0000"}), assert updated
- `test_update_value_group` -- val.update(value_group="warm"), assert updated
- `test_update_value_group_to_none` -- val.update(value_group=None), assert None
- `test_update_rejects_unknown_field` -- pytest.raises(TypeError)

**TestProductAttributeValueCreate:**
- `test_create_valid` -- ProductAttributeValueBuilder().build(), assert isinstance(id, uuid.UUID)
- `test_create_has_correct_refs` -- builder with explicit IDs, assert product_id, attribute_id, attribute_value_id all match

All tests plain `def test_...` per D-08. Use builders per D-06.
  </action>
  <verify>
    <automated>cd C:/Users/Sanjar/Desktop/loyality/backend && uv run pytest tests/unit/modules/catalog/domain/test_attribute.py -x -q --no-cov --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `test_attribute.py` has classes TestAttributeCreate, TestAttributeUpdate, TestAttributeGuard, TestAttributeProperties, TestAttributeValueCreate, TestAttributeValueUpdate, TestProductAttributeValueCreate
    - All tests pass with exit code 0
    - grep "class TestAttributeCreate" test_attribute.py returns match
    - grep "class TestAttributeValueCreate" test_attribute.py returns match
    - grep "class TestProductAttributeValueCreate" test_attribute.py returns match
    - grep "AttributeBuilder" test_attribute.py returns match (builders used per D-06)
    - grep "BehaviorFlags" test_attribute.py returns match (behavior flag testing)
    - No async test functions
  </acceptance_criteria>
  <done>
    Attribute tests cover create (10 cases), update (11 cases), guard (2 cases), properties (5 cases). AttributeValue tests cover create (7 cases), update (8 cases). ProductAttributeValue tests cover create (2 cases). Total ~45 tests. All pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: AttributeTemplate, TemplateAttributeBinding, and AttributeGroup tests</name>
  <files>
    backend/tests/unit/modules/catalog/domain/test_attribute_template.py
    backend/tests/unit/modules/catalog/domain/test_attribute_group.py
  </files>
  <read_first>
    backend/src/modules/catalog/domain/entities.py (lines 499-786 for AttributeTemplate, TemplateAttributeBinding, AttributeGroup)
    backend/src/modules/catalog/domain/entities.py (lines 102-137 for _FILTER_SETTINGS_MAX_KEYS, _FILTER_SETTINGS_ALLOWED_KEYS, _validate_filter_settings)
    backend/src/modules/catalog/domain/exceptions.py (AttributeTemplateHasCategoryReferencesError)
    backend/tests/factories/attribute_template_builder.py
    backend/tests/factories/attribute_group_builder.py
  </read_first>
  <action>
Create `backend/tests/unit/modules/catalog/domain/test_attribute_template.py` with these test classes:

Include helper:
```python
def _i18n(en: str, ru: str | None = None) -> dict[str, str]:
    return {"en": en, "ru": ru or en}
```

**TestAttributeTemplateCreate:**
- `test_create_valid` -- AttributeTemplateBuilder().build(), assert isinstance(id, uuid.UUID), sort_order==0
- `test_create_with_description` -- pass description_i18n, assert set
- `test_create_rejects_missing_locale` -- pytest.raises(MissingRequiredLocalesError), name_i18n={"en": "Only English"} (Pitfall 5)
- `test_create_rejects_blank_i18n_values` -- pytest.raises(ValueError)
- `test_create_rejects_negative_sort_order` -- pytest.raises(ValueError, match="sort_order must be non-negative")
- `test_create_custom_sort_order` -- sort_order=5, assert sort_order==5

**TestAttributeTemplateUpdate:**
- `test_update_name_i18n` -- template.update(name_i18n=_i18n("New")), assert updated
- `test_update_description_i18n` -- template.update(description_i18n=_i18n("Desc")), assert updated
- `test_update_sort_order` -- template.update(sort_order=10), assert updated
- `test_update_rejects_unknown_field` -- pytest.raises(TypeError), template.update(unknown="x")

**TestAttributeTemplateGuard** (Pitfall 2):
- `test_direct_code_assignment_raises` -- pytest.raises(AttributeError, match="Cannot set 'code' directly")

**TestAttributeTemplateDeletion:**
- `test_deletable_when_no_category_refs` -- template.validate_deletable(has_category_refs=False), no exception
- `test_not_deletable_when_has_category_refs` -- pytest.raises(AttributeTemplateHasCategoryReferencesError)

**TestTemplateAttributeBindingCreate:**
- `test_create_valid` -- TemplateAttributeBindingBuilder().build(), assert isinstance(id, uuid.UUID)
- `test_create_default_requirement_level` -- assert binding.requirement_level == RequirementLevel.OPTIONAL
- `test_create_with_requirement_level` -- pass RequirementLevel.REQUIRED, assert set
- `test_create_rejects_negative_sort_order` -- pytest.raises(ValueError, match="sort_order must be non-negative")
- `test_create_with_valid_filter_settings` -- pass filter_settings={"widget": "slider", "min": 0, "max": 100}, assert set
  Use actual allowed keys from _FILTER_SETTINGS_ALLOWED_KEYS: widget, min, max, step, unit, collapsed, behavior, display, options
- `test_create_rejects_invalid_filter_settings_keys` -- pytest.raises(ValueError, match="unknown keys"), pass filter_settings={"unknown_key": "value"}
- `test_create_rejects_non_dict_filter_settings` -- pytest.raises(ValueError, match="must be a JSON object"), pass filter_settings="not a dict"
  NOTE: _validate_filter_settings checks `isinstance(settings, dict)` at line 124
- `test_create_rejects_too_many_filter_keys` -- pytest.raises(ValueError, match="too many keys"), pass dict with 21 keys (max is 20 per _FILTER_SETTINGS_MAX_KEYS)
  Generate 21 keys by repeating allowed keys won't work (only 9 allowed keys). Instead, this test may need to be reconsidered. Read the validation: it checks key count BEFORE checking allowed keys. So you could pass 21 unique valid keys -- but there are only 9 allowed keys. So test with 21 arbitrary keys, they'll fail the count check first.

**TestTemplateAttributeBindingUpdate:**
- `test_update_sort_order` -- binding.update(sort_order=5), assert updated
- `test_update_requirement_level` -- binding.update(requirement_level=RequirementLevel.REQUIRED), assert updated
- `test_update_filter_settings` -- binding.update(filter_settings={"widget": "checkbox"}), assert updated
- `test_update_rejects_unknown_field` -- pytest.raises(TypeError)

Create `backend/tests/unit/modules/catalog/domain/test_attribute_group.py` with these test classes:

NOTE: AttributeGroup.update() uses EXPLICIT kwargs (not **kwargs). Read the method signature carefully.
The method is: `def update(self, name_i18n=..., sort_order=...)` with Ellipsis sentinels.

**TestAttributeGroupCreate:**
- `test_create_valid` -- AttributeGroupBuilder().build(), assert isinstance(id, uuid.UUID), sort_order==0
- `test_create_rejects_missing_locale` -- pytest.raises(MissingRequiredLocalesError), name_i18n={"en": "Only English"}
- `test_create_rejects_blank_i18n_values` -- pytest.raises(ValueError)
- `test_create_rejects_negative_sort_order` -- pytest.raises(ValueError, match="sort_order must be non-negative")
- `test_create_custom_sort_order` -- sort_order=3, assert sort_order==3
- `test_create_rejects_invalid_code` -- pytest.raises(ValueError), code="BAD CODE!" (code validated via _validate_slug)

**TestAttributeGroupUpdate:**
- `test_update_name_i18n` -- group.update(name_i18n=_i18n("New")), assert updated
- `test_update_sort_order` -- group.update(sort_order=10), assert updated
- `test_update_rejects_empty_name_i18n` -- pytest.raises(ValueError), group.update(name_i18n={"en": "", "ru": ""})
- `test_update_name_i18n_omitted_keeps_current` -- group.update(sort_order=5), assert name_i18n unchanged (Ellipsis sentinel test)
  Since update() uses explicit kwargs with Ellipsis defaults, not providing name_i18n means it stays as `...` and is skipped.

**TestAttributeGroupGuard** (Pitfall 2):
- `test_direct_code_assignment_raises` -- pytest.raises(AttributeError, match="Cannot set 'code' directly")

IMPORTANT: Read the actual source code for AttributeGroup.update() and TemplateAttributeBinding.update() BEFORE writing tests. The interfaces above describe the expected behavior, but confirm the exact validation logic (error messages, edge cases) from the source.

All tests plain `def test_...` per D-08. Use builders per D-06.
  </action>
  <verify>
    <automated>cd C:/Users/Sanjar/Desktop/loyality/backend && uv run pytest tests/unit/modules/catalog/domain/test_attribute_template.py tests/unit/modules/catalog/domain/test_attribute_group.py -x -q --no-cov --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `test_attribute_template.py` has classes TestAttributeTemplateCreate, TestAttributeTemplateUpdate, TestAttributeTemplateGuard, TestAttributeTemplateDeletion, TestTemplateAttributeBindingCreate, TestTemplateAttributeBindingUpdate
    - `test_attribute_group.py` has classes TestAttributeGroupCreate, TestAttributeGroupUpdate, TestAttributeGroupGuard
    - All tests pass with exit code 0
    - grep "class TestAttributeTemplateCreate" test_attribute_template.py returns match
    - grep "class TestTemplateAttributeBindingCreate" test_attribute_template.py returns match
    - grep "class TestAttributeGroupCreate" test_attribute_group.py returns match
    - grep "AttributeTemplateBuilder" test_attribute_template.py returns match (builders used per D-06)
    - grep "AttributeGroupBuilder" test_attribute_group.py returns match
    - No async test functions
  </acceptance_criteria>
  <done>
    AttributeTemplate tests cover create (6 cases), update (4 cases), guard (1 case), deletion (2 cases). TemplateAttributeBinding tests cover create (8 cases including filter_settings validation with correct max=20 keys and 9 allowed keys), update (4 cases). AttributeGroup tests cover create (6 cases), update (4 cases including Ellipsis sentinel), guard (1 case). Total ~36 tests. All pass.
  </done>
</task>

</tasks>

<verification>
Run all domain tests including new files:
```bash
cd C:/Users/Sanjar/Desktop/loyality/backend && uv run pytest tests/unit/modules/catalog/domain/ -x -q --no-cov --tb=short
```
Expected: All tests in test_attribute.py, test_attribute_template.py, test_attribute_group.py, and all existing tests pass together.
</verification>

<success_criteria>
1. test_attribute.py exists with >= 35 tests covering Attribute, AttributeValue, ProductAttributeValue
2. test_attribute_template.py exists with >= 20 tests covering AttributeTemplate, TemplateAttributeBinding
3. test_attribute_group.py exists with >= 9 tests covering AttributeGroup
4. All tests pass: `uv run pytest tests/unit/modules/catalog/domain/ -x -q --no-cov` exits 0
5. No async test functions
6. Builders from Phase 1 used (AttributeBuilder, AttributeValueBuilder, ProductAttributeValueBuilder, AttributeTemplateBuilder, TemplateAttributeBindingBuilder, AttributeGroupBuilder)
</success_criteria>

<output>
After completion, create `.planning/phases/02-value-objects-entity-foundations/02-03-SUMMARY.md`
</output>
