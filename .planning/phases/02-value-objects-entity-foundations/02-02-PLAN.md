---
phase: 02-value-objects-entity-foundations
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/unit/modules/catalog/domain/test_product.py
  - backend/tests/unit/modules/catalog/domain/test_variant.py
  - backend/tests/unit/modules/catalog/domain/test_sku.py
autonomous: true
requirements: [DOM-01]
must_haves:
  truths:
    - "Product.create() auto-creates one default variant and emits ProductCreatedEvent"
    - "Product.create() rejects invalid slug, empty title_i18n, missing locales"
    - "Product.update() mutates allowed fields, emits ProductUpdatedEvent, rejects brand_id=None and primary_category_id=None"
    - "Product.__setattr__ guard prevents direct status mutation"
    - "Product.soft_delete() cascades deleted_at to all variants and their SKUs"
    - "Product.add_variant() creates variant and emits VariantAddedEvent"
    - "Product.remove_variant() soft-deletes variant, emits VariantDeletedEvent, rejects removing last variant"
    - "Product.add_sku() creates SKU with variant_hash, emits SKUAddedEvent, rejects duplicate variant_hash"
    - "Product.remove_sku() soft-deletes SKU, emits SKUDeletedEvent"
    - "Product.find_variant() and find_sku() return None for deleted or unknown IDs"
    - "Product.compute_variant_hash() is deterministic and order-independent"
    - "Product.tags and Product.variants return tuples (read-only)"
    - "ProductVariant.create() validates i18n, sort_order, price/currency"
    - "ProductVariant.update() mutates allowed fields, rejects unknown fields"
    - "ProductVariant.soft_delete() cascades to SKUs"
    - "SKU.__attrs_post_init__() validates compare_at_price > price and same currency"
    - "SKU.update() mutates allowed fields with cross-field validation"
    - "SKU.soft_delete() sets deleted_at and is idempotent"
  artifacts:
    - path: "backend/tests/unit/modules/catalog/domain/test_product.py"
      provides: "Unit tests for Product aggregate create, update, guard, soft-delete, variant/SKU management"
      min_lines: 200
    - path: "backend/tests/unit/modules/catalog/domain/test_variant.py"
      provides: "Unit tests for ProductVariant create, update, soft-delete"
      min_lines: 60
    - path: "backend/tests/unit/modules/catalog/domain/test_sku.py"
      provides: "Unit tests for SKU construction validation, update, soft-delete"
      min_lines: 60
  key_links:
    - from: "backend/tests/unit/modules/catalog/domain/test_product.py"
      to: "backend/src/modules/catalog/domain/entities.py"
      via: "imports Product, uses ProductBuilder"
      pattern: "from src.modules.catalog.domain.entities import Product"
    - from: "backend/tests/unit/modules/catalog/domain/test_variant.py"
      to: "backend/src/modules/catalog/domain/entities.py"
      via: "imports ProductVariant, uses ProductVariantBuilder"
      pattern: "from src.modules.catalog.domain.entities import ProductVariant"
    - from: "backend/tests/unit/modules/catalog/domain/test_sku.py"
      to: "backend/src/modules/catalog/domain/entities.py"
      via: "imports SKU, uses SKUBuilder and ProductBuilder"
      pattern: "from src.modules.catalog.domain.entities import.*SKU"
---

<objective>
Unit tests for the Product aggregate root (including variant and SKU child entity management), the standalone ProductVariant entity, and the SKU child entity.

Purpose: Prove the most complex aggregate in the catalog domain works correctly -- factory methods, update methods, variant/SKU lifecycle, soft-delete cascading, variant hash computation, domain event emission, and all validation guards. This is the highest-priority work per D-04 (product creation, variant/SKU generation are priorities 1 and 2).

Output: Three test files covering Product aggregate behavior, ProductVariant standalone operations, and SKU validation/update.
</objective>

<execution_context>
@C:/Users/Sanjar/Desktop/loyality/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Sanjar/Desktop/loyality/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-value-objects-entity-foundations/02-CONTEXT.md
@.planning/phases/02-value-objects-entity-foundations/02-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From backend/src/modules/catalog/domain/entities.py (Product, lines 1663-2220):
- Product.create(slug, title_i18n, brand_id, primary_category_id, description_i18n=None, supplier_id=None, source_url=None, country_of_origin=None, tags=None, product_id=None) -> Product
  - Auto-creates one default variant with name_i18n = title_i18n
  - Emits ProductCreatedEvent
- Product._UPDATABLE_FIELDS = {"title_i18n", "slug", "brand_id", "primary_category_id", "description_i18n", "supplier_id", "source_url", "country_of_origin", "tags"}
- Product.update(**kwargs) -> str | None — returns old_slug when slug changes, emits ProductUpdatedEvent
  - brand_id=None and primary_category_id=None are REJECTED (ValueError "brand_id cannot be None")
- Product.soft_delete() — sets deleted_at, cascades to variants->SKUs, rejects if status==PUBLISHED (CannotDeletePublishedProductError), emits ProductDeletedEvent, idempotent
- Product.transition_status(new_status: ProductStatus) -> None — FSM transitions (Phase 3 scope, basic guard is DOM-01)
- Product.add_variant(name_i18n, description_i18n=None, sort_order=0, default_price=None, default_currency=DEFAULT_CURRENCY) -> ProductVariant, emits VariantAddedEvent
- Product.find_variant(variant_id) -> ProductVariant | None — returns None for deleted
- Product.remove_variant(variant_id) -> None — soft-deletes, emits VariantDeletedEvent, raises LastVariantRemovalError if only 1 active variant, raises VariantNotFoundError
- Product.add_sku(variant_id, sku_code, price=None, compare_at_price=None, is_active=True, variant_attributes=None) -> SKU, emits SKUAddedEvent, raises DuplicateVariantCombinationError on hash collision, raises VariantNotFoundError
- Product.find_sku(sku_id) -> SKU | None — returns None for deleted
- Product.remove_sku(sku_id) -> None — soft-deletes, emits SKUDeletedEvent, raises SKUNotFoundError
- Product.compute_variant_hash(variant_id, variant_attributes) — static, deterministic, order-independent
- _PRODUCT_GUARDED_FIELDS = frozenset({"status"})
- Product.tags -> tuple[str, ...] (read-only property)
- Product.variants -> tuple[ProductVariant, ...] (read-only property)

From backend/src/modules/catalog/domain/entities.py (ProductVariant, lines 1406-1558):
- ProductVariant.create(product_id, name_i18n, description_i18n=None, sort_order=0, default_price=None, default_currency=DEFAULT_CURRENCY) -> ProductVariant
  - Validates i18n, sort_order, price/currency pairing
- ProductVariant._UPDATABLE_FIELDS = {"name_i18n", "sort_order", "default_price", "default_currency", "description_i18n"}
- ProductVariant.update(**kwargs) — validates name_i18n, sort_order, default_price+default_currency combo
- ProductVariant.soft_delete() — sets deleted_at, cascades to all SKUs
- ProductVariant.skus -> tuple[SKU, ...] (read-only property)

From backend/src/modules/catalog/domain/entities.py (SKU, lines 1267-1398):
- SKU constructed internally by Product.add_sku(), not via create()
- SKU.__attrs_post_init__() validates: compare_at_price without price raises, compare_at_price <= price raises, currency mismatch raises
- SKU._UPDATABLE_FIELDS = {"sku_code", "price", "compare_at_price", "is_active"}
- SKU.update(**kwargs) — with cross-field validation on price/compare_at
- SKU.soft_delete() — sets deleted_at, idempotent

From backend/src/modules/catalog/domain/events.py:
- ProductCreatedEvent, ProductUpdatedEvent, ProductDeletedEvent, ProductStatusChangedEvent
- VariantAddedEvent, VariantDeletedEvent
- SKUAddedEvent, SKUDeletedEvent

From backend/src/modules/catalog/domain/exceptions.py:
- CannotDeletePublishedProductError, InvalidStatusTransitionError
- DuplicateVariantCombinationError, LastVariantRemovalError, VariantNotFoundError
- SKUNotFoundError

From builders:
- ProductBuilder().with_slug("x").with_brand_id(id).with_category_id(id).build() -> Product
- ProductVariantBuilder().with_product_id(id).with_name_i18n({...}).build() -> ProductVariant
- SKUBuilder().for_product(p).for_variant(v_id).with_sku_code("SKU-1").with_price(Money(...)).build() -> SKU
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Product aggregate tests</name>
  <files>
    backend/tests/unit/modules/catalog/domain/test_product.py
  </files>
  <read_first>
    backend/src/modules/catalog/domain/entities.py (lines 1663-2220 for Product)
    backend/src/modules/catalog/domain/events.py
    backend/src/modules/catalog/domain/exceptions.py
    backend/tests/factories/product_builder.py
    backend/tests/factories/sku_builder.py
  </read_first>
  <action>
Create `backend/tests/unit/modules/catalog/domain/test_product.py` with these test classes per D-07.

Include helper at top:
```python
def _i18n(en: str, ru: str | None = None) -> dict[str, str]:
    return {"en": en, "ru": ru or en}
```

**TestProductCreate** (per D-04 priority 1 — product creation):
- `test_create_valid` — ProductBuilder().build(), assert isinstance(id, uuid.UUID), status == ProductStatus.DRAFT
- `test_create_auto_creates_default_variant` — per Pitfall 3: assert len(product.variants) == 1, default variant name_i18n matches product title_i18n
- `test_create_emits_product_created_event` — assert len(product.domain_events) >= 1, isinstance(product.domain_events[0], ProductCreatedEvent)
- `test_create_rejects_invalid_slug` — pytest.raises(ValueError, match="slug must be non-empty"), Product.create(slug="Bad Slug!", title_i18n=_i18n("T"), brand_id=uuid.uuid4(), primary_category_id=uuid.uuid4())
- `test_create_rejects_empty_slug` — pytest.raises(ValueError), slug=""
- `test_create_rejects_empty_title_i18n` — pytest.raises(ValueError), title_i18n={"en": "", "ru": "Valid"}
- `test_create_rejects_missing_locale` — pytest.raises (MissingRequiredLocalesError), title_i18n={"en": "Only English"}
- `test_create_with_optional_fields` — ProductBuilder().with_supplier_id(uuid.uuid4()).with_source_url("https://poizon.com/123").with_country_of_origin("CN").with_tags(["sale", "new"]).build(), assert all fields set
- `test_tags_returns_tuple` — product.tags is tuple (read-only per D-03)
- `test_variants_returns_tuple` — product.variants is tuple (read-only)

**TestProductUpdate:**
- `test_update_title_i18n` — product.update(title_i18n=_i18n("New Title")), assert updated
- `test_update_slug_returns_old_slug` — result = product.update(slug="new-slug"), assert result == old slug string, assert product.slug == "new-slug"
- `test_update_without_slug_returns_none` — result = product.update(title_i18n=_i18n("X")), assert result is None
- `test_update_emits_product_updated_event` — clear events, product.update(title_i18n=_i18n("X")), assert ProductUpdatedEvent in events
- `test_update_rejects_brand_id_none` — pytest.raises(ValueError, match="brand_id cannot be None"), product.update(brand_id=None)
- `test_update_rejects_primary_category_id_none` — pytest.raises(ValueError, match="primary_category_id cannot be None"), product.update(primary_category_id=None)
- `test_update_rejects_unknown_field` — pytest.raises(TypeError), product.update(unknown="x")
- `test_update_tags` — product.update(tags=["premium"]), assert product.tags == ("premium",)

**TestProductGuard:**
- `test_direct_status_assignment_raises` — pytest.raises(AttributeError, match="Cannot set 'status' directly"), product.status = ProductStatus.PUBLISHED

**TestProductSoftDelete:**
- `test_soft_delete_sets_deleted_at` — product.soft_delete(), assert product.deleted_at is not None
- `test_soft_delete_cascades_to_variants` — add extra variant, soft_delete, all variants have deleted_at set
- `test_soft_delete_cascades_to_skus` — add SKU, soft_delete, SKU has deleted_at set
- `test_soft_delete_emits_event` — clear events, product.soft_delete(), assert ProductDeletedEvent in events
- `test_soft_delete_idempotent` — product.soft_delete() twice, no error, deleted_at timestamp same
- `test_soft_delete_rejects_published_product` — transition product to PUBLISHED (use transition_status chain: DRAFT->ENRICHING->READY_FOR_REVIEW->PUBLISHED), pytest.raises(CannotDeletePublishedProductError)

**TestProductVariantManagement** (per D-04 priority 2 — variant generation):
- `test_add_variant` — product.add_variant(name_i18n=_i18n("Red")), assert len(product.variants) == 2 (1 default + 1 new)
- `test_add_variant_emits_event` — clear events, product.add_variant(...), assert VariantAddedEvent
- `test_find_variant_active` — variant = product.add_variant(...), found = product.find_variant(variant.id), assert found is variant
- `test_find_variant_returns_none_for_deleted` — variant = product.add_variant(...), variant.soft_delete(), assert product.find_variant(variant.id) is None
- `test_find_variant_returns_none_for_unknown_id` — assert product.find_variant(uuid.uuid4()) is None
- `test_remove_variant` — add second variant, product.remove_variant(second.id), assert second.deleted_at is not None
- `test_remove_variant_emits_event` — clear events, remove variant, assert VariantDeletedEvent
- `test_remove_last_variant_raises` — only 1 default variant, pytest.raises(LastVariantRemovalError), product.remove_variant(default_variant.id)
- `test_remove_unknown_variant_raises` — pytest.raises(VariantNotFoundError), product.remove_variant(uuid.uuid4())

**TestProductSKUManagement** (per D-04 priority 2):
- `test_add_sku` — SKUBuilder().for_product(product).build(), assert len(product.variants[0]._skus) >= 1 or check via find_sku
- `test_add_sku_emits_event` — clear events, add_sku, assert SKUAddedEvent
- `test_add_sku_duplicate_hash_raises` — add two SKUs with same variant_attributes, pytest.raises(DuplicateVariantCombinationError)
- `test_add_sku_unknown_variant_raises` — pytest.raises(VariantNotFoundError), product.add_sku(uuid.uuid4(), sku_code="X")
- `test_find_sku_active` — sku = add_sku, found = product.find_sku(sku.id), assert found is sku
- `test_find_sku_returns_none_for_deleted` — sku.soft_delete(), assert product.find_sku(sku.id) is None
- `test_find_sku_returns_none_for_unknown` — assert product.find_sku(uuid.uuid4()) is None
- `test_remove_sku` — sku = add_sku, product.remove_sku(sku.id), assert sku.deleted_at is not None
- `test_remove_sku_emits_event` — clear events, remove_sku, assert SKUDeletedEvent
- `test_remove_unknown_sku_raises` — pytest.raises(SKUNotFoundError)

**TestProductVariantHash:**
- `test_compute_variant_hash_deterministic` — same inputs produce same hash
- `test_compute_variant_hash_order_independent` — [(a1,v1),(a2,v2)] same hash as [(a2,v2),(a1,v1)]
- `test_compute_variant_hash_different_for_different_variants` — different variant_id produces different hash
- `test_compute_variant_hash_different_for_different_attributes` — different attribute values produce different hash

All tests plain `def test_...` per D-08. Use ProductBuilder, SKUBuilder per D-06. Clear domain_events after build() where testing subsequent events to avoid conflation with ProductCreatedEvent.
  </action>
  <verify>
    <automated>cd C:/Users/Sanjar/Desktop/loyality/backend && uv run pytest tests/unit/modules/catalog/domain/test_product.py -x -q --no-cov --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `test_product.py` has classes TestProductCreate, TestProductUpdate, TestProductGuard, TestProductSoftDelete, TestProductVariantManagement, TestProductSKUManagement, TestProductVariantHash
    - All tests pass with exit code 0
    - grep "class TestProductCreate" test_product.py returns match
    - grep "ProductCreatedEvent" test_product.py returns match (event emission tested)
    - grep "ProductBuilder" test_product.py returns match (builders used per D-06)
    - grep "DuplicateVariantCombinationError" test_product.py returns match (hash collision tested)
    - No async test functions
  </acceptance_criteria>
  <done>
    Product aggregate tests cover create (10 cases), update (8 cases), guard (1 case), soft-delete (6 cases), variant management (9 cases), SKU management (10 cases), variant hash (4 cases). Total ~48 tests. All pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: ProductVariant and SKU entity tests</name>
  <files>
    backend/tests/unit/modules/catalog/domain/test_variant.py
    backend/tests/unit/modules/catalog/domain/test_sku.py
  </files>
  <read_first>
    backend/src/modules/catalog/domain/entities.py (lines 1267-1558 for SKU and ProductVariant)
    backend/src/modules/catalog/domain/value_objects.py (Money for SKU price validation)
    backend/tests/factories/variant_builder.py
    backend/tests/factories/sku_builder.py
    backend/tests/factories/product_builder.py
  </read_first>
  <action>
Create `backend/tests/unit/modules/catalog/domain/test_variant.py` with these test classes:

Include helper:
```python
def _i18n(en: str, ru: str | None = None) -> dict[str, str]:
    return {"en": en, "ru": ru or en}
```

**TestProductVariantCreate:**
- `test_create_valid` — ProductVariantBuilder().build(), assert isinstance(id, uuid.UUID), sort_order==0
- `test_create_with_name_i18n` — ProductVariantBuilder().with_name_i18n(_i18n("Red")).build(), assert name_i18n==_i18n("Red")
- `test_create_rejects_missing_locale` — pytest.raises(MissingRequiredLocalesError), ProductVariant.create(product_id=uuid.uuid4(), name_i18n={"en": "Red"})
- `test_create_rejects_blank_i18n_values` — pytest.raises(ValueError), name_i18n={"en": "", "ru": "Valid"}
- `test_create_rejects_negative_sort_order` — pytest.raises(ValueError, match="sort_order must be non-negative")
- `test_create_with_default_price` — ProductVariantBuilder().with_price(Money(amount=1000, currency="RUB")).build(), assert default_price.amount == 1000
- `test_create_with_custom_currency` — ProductVariantBuilder().with_currency("USD").build()
- `test_skus_returns_tuple` — variant.skus is tuple

**TestProductVariantUpdate:**
- `test_update_name_i18n` — variant.update(name_i18n=_i18n("Blue")), assert updated
- `test_update_sort_order` — variant.update(sort_order=5), assert sort_order==5
- `test_update_default_price_and_currency` — variant.update(default_price=Money(2000, "RUB"), default_currency="RUB"), assert updated
- `test_update_rejects_unknown_field` — pytest.raises(TypeError), variant.update(unknown="x")
- `test_update_rejects_blank_name_i18n` — pytest.raises(ValueError), variant.update(name_i18n={"en": "", "ru": "Valid"})
- `test_update_rejects_negative_sort_order` — pytest.raises(ValueError)

**TestProductVariantSoftDelete:**
- `test_soft_delete_sets_deleted_at` — variant.soft_delete(), assert deleted_at is not None
- `test_soft_delete_cascades_to_skus` — create product, add SKU to variant, variant.soft_delete(), assert SKU.deleted_at is not None
- `test_soft_delete_idempotent` — variant.soft_delete() twice, no error

Create `backend/tests/unit/modules/catalog/domain/test_sku.py` with these test classes:

**TestSKUConstruction** (per Pitfall 4 — SKU only via Product.add_sku()):
- `test_create_via_product_add_sku` — SKUBuilder().build(), assert isinstance(sku.id, uuid.UUID), sku.sku_code starts with "SKU-"
- `test_create_with_price` — SKUBuilder().with_price(Money(1000, "RUB")).build(), assert sku.price.amount == 1000
- `test_create_with_valid_compare_at_price` — SKUBuilder().with_price(Money(1000, "RUB")).with_compare_at_price(Money(2000, "RUB")).build(), assert compare_at_price.amount == 2000
- `test_rejects_compare_at_without_price` — Read the SKU.__attrs_post_init__ to determine exact behavior: if compare_at_price is set but price is None, should raise ValueError. Test via Product.add_sku() directly with compare_at_price=Money(2000, "RUB") and price=None. pytest.raises(ValueError)
- `test_rejects_compare_at_less_than_price` — Product.add_sku() with price=Money(2000, "RUB"), compare_at_price=Money(1000, "RUB"). pytest.raises(ValueError)
- `test_rejects_compare_at_equal_to_price` — pytest.raises(ValueError)
- `test_rejects_compare_at_currency_mismatch` — price=Money(1000, "RUB"), compare_at_price=Money(2000, "USD"). pytest.raises(ValueError)
- `test_create_inactive` — SKUBuilder().as_inactive().build(), assert sku.is_active is False

**TestSKUUpdate:**
- `test_update_sku_code` — sku.update(sku_code="NEW-CODE"), assert sku.sku_code == "NEW-CODE"
- `test_update_price` — sku.update(price=Money(5000, "RUB")), assert updated
- `test_update_is_active` — sku.update(is_active=False), assert sku.is_active is False
- `test_update_rejects_unknown_field` — pytest.raises(TypeError), sku.update(unknown="x")
- `test_update_compare_at_price` — sku with price, sku.update(compare_at_price=Money(higher, "RUB")), assert updated

**TestSKUSoftDelete:**
- `test_soft_delete_sets_deleted_at` — sku.soft_delete(), assert sku.deleted_at is not None
- `test_soft_delete_idempotent` — sku.soft_delete() twice, no error, same deleted_at timestamp

All tests plain `def test_...` per D-08. Use builders per D-06.
  </action>
  <verify>
    <automated>cd C:/Users/Sanjar/Desktop/loyality/backend && uv run pytest tests/unit/modules/catalog/domain/test_variant.py tests/unit/modules/catalog/domain/test_sku.py -x -q --no-cov --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `test_variant.py` has classes TestProductVariantCreate, TestProductVariantUpdate, TestProductVariantSoftDelete
    - `test_sku.py` has classes TestSKUConstruction, TestSKUUpdate, TestSKUSoftDelete
    - All tests pass with exit code 0
    - grep "class TestProductVariantCreate" test_variant.py returns match
    - grep "class TestSKUConstruction" test_sku.py returns match
    - grep "SKUBuilder" test_sku.py returns match (builders used per D-06)
    - grep "ProductVariantBuilder" test_variant.py returns match
    - No async test functions
  </acceptance_criteria>
  <done>
    ProductVariant tests cover create (8 cases), update (6 cases), soft-delete (3 cases). SKU tests cover construction validation (8 cases), update (5 cases), soft-delete (2 cases). Total ~32 tests. All pass.
  </done>
</task>

</tasks>

<verification>
Run all domain tests including new files:
```bash
cd C:/Users/Sanjar/Desktop/loyality/backend && uv run pytest tests/unit/modules/catalog/domain/ -x -q --no-cov --tb=short
```
Expected: All tests in test_product.py, test_variant.py, test_sku.py, and existing tests pass together.
</verification>

<success_criteria>
1. test_product.py exists with >= 40 tests covering Product create, update, guard, soft-delete, variant management, SKU management, variant hash
2. test_variant.py exists with >= 15 tests covering ProductVariant create, update, soft-delete
3. test_sku.py exists with >= 12 tests covering SKU construction, update, soft-delete
4. All tests pass: `uv run pytest tests/unit/modules/catalog/domain/ -x -q --no-cov` exits 0
5. No async test functions
6. Builders from Phase 1 used (ProductBuilder, ProductVariantBuilder, SKUBuilder)
</success_criteria>

<output>
After completion, create `.planning/phases/02-value-objects-entity-foundations/02-02-SUMMARY.md`
</output>
