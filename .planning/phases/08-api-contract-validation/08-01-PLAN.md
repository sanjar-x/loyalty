---
phase: 08-api-contract-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/e2e/api/v1/catalog/__init__.py
  - backend/tests/e2e/api/v1/catalog/conftest.py
  - backend/tests/e2e/api/v1/catalog/test_brands.py
  - backend/tests/e2e/api/v1/catalog/test_categories.py
  - backend/tests/e2e/api/v1/catalog/test_attributes.py
  - backend/tests/e2e/api/v1/catalog/test_attribute_values.py
  - backend/tests/e2e/api/v1/catalog/test_attribute_templates.py
autonomous: true
requirements: [API-01]
user_setup: []

must_haves:
  truths:
    - "Brand CRUD endpoints return correct status codes (201/200/204) and camelCase response shapes"
    - "Category CRUD + tree/bulk endpoints return correct status codes and response shapes"
    - "Attribute CRUD + usage endpoints return correct status codes and response shapes"
    - "AttributeValue CRUD + activate/deactivate/reorder endpoints return correct status codes"
    - "AttributeTemplate CRUD + binding endpoints return correct status codes and response shapes"
    - "Each endpoint returns correct error status (404 for not-found, 422 for invalid input)"
  artifacts:
    - path: "backend/tests/e2e/api/v1/catalog/conftest.py"
      provides: "Shared E2E helper fixtures for creating prerequisite entities via API"
      min_lines: 50
    - path: "backend/tests/e2e/api/v1/catalog/test_brands.py"
      provides: "Brand endpoint contract tests"
      contains: "test_create_brand"
    - path: "backend/tests/e2e/api/v1/catalog/test_categories.py"
      provides: "Category endpoint contract tests"
      contains: "test_create_category"
    - path: "backend/tests/e2e/api/v1/catalog/test_attributes.py"
      provides: "Attribute endpoint contract tests"
      contains: "test_create_attribute"
    - path: "backend/tests/e2e/api/v1/catalog/test_attribute_values.py"
      provides: "AttributeValue endpoint contract tests"
      contains: "test_add_attribute_value"
    - path: "backend/tests/e2e/api/v1/catalog/test_attribute_templates.py"
      provides: "AttributeTemplate + binding endpoint contract tests"
      contains: "test_create_template"
  key_links:
    - from: "backend/tests/e2e/api/v1/catalog/conftest.py"
      to: "all test files in catalog/"
      via: "shared fixtures imported by pytest"
      pattern: "async def create_brand"
---

<objective>
Test foundational catalog admin endpoints: Brands, Categories, Attributes, AttributeValues, and AttributeTemplates.

Purpose: Prove these 5 routers (40 endpoints) return correct HTTP status codes, camelCase response shapes, and error responses. These entities are prerequisites for product tests.
Output: Shared conftest.py + 5 test files covering happy-path and error-path for each endpoint.
</objective>

<execution_context>
@C:/Users/Sanjar/Desktop/loyality/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Sanjar/Desktop/loyality/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

[Relevant source files:]
@backend/tests/e2e/conftest.py
@backend/tests/e2e/api/v1/test_brands.py
@backend/tests/e2e/api/v1/test_categories.py
@backend/src/modules/catalog/presentation/router_brands.py
@backend/src/modules/catalog/presentation/router_categories.py
@backend/src/modules/catalog/presentation/router_attributes.py
@backend/src/modules/catalog/presentation/router_attribute_values.py
@backend/src/modules/catalog/presentation/router_attribute_templates.py
@backend/src/modules/catalog/presentation/schemas.py
@backend/src/api/exceptions/handlers.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create catalog E2E test directory and shared conftest with helper fixtures</name>
  <files>backend/tests/e2e/api/v1/catalog/__init__.py, backend/tests/e2e/api/v1/catalog/conftest.py</files>
  <read_first>backend/tests/e2e/conftest.py, backend/tests/e2e/api/v1/test_brands.py, backend/tests/e2e/api/v1/test_categories.py, backend/src/modules/catalog/presentation/schemas.py, backend/src/modules/catalog/presentation/router_brands.py, backend/src/modules/catalog/presentation/router_categories.py, backend/src/modules/catalog/presentation/router_attributes.py, backend/src/modules/catalog/presentation/router_attribute_values.py, backend/src/modules/catalog/presentation/router_attribute_templates.py</read_first>
  <action>
  1. Create `backend/tests/e2e/api/v1/catalog/__init__.py` -- empty file for pytest discovery.

  2. Create `backend/tests/e2e/api/v1/catalog/conftest.py` with async helper fixtures that create prerequisite entities via API calls. Each helper takes `admin_client` as parameter and returns the created entity's response dict. These are NOT pytest fixtures -- they are plain async functions called within tests. This avoids fixture ordering issues and lets each test create exactly what it needs.

  Helper functions to create:
  - `async def create_brand(client, *, name="Test Brand", slug="test-brand", logo_url=None) -> dict` -- POST /api/v1/catalog/brands, returns response JSON (has `id` field)
  - `async def create_category(client, *, name_i18n=None, slug="test-category", parent_id=None, template_id=None) -> dict` -- POST /api/v1/catalog/categories, returns response JSON (has `id` field). Default name_i18n: `{"ru": "Тест", "en": "Test"}`
  - `async def create_attribute(client, *, code="test_attr", slug="test-attr", name_i18n=None, data_type="string", ui_type="dropdown", is_dictionary=True, level="product") -> dict` -- POST /api/v1/catalog/attributes, returns response JSON (has `id` field). Default name_i18n: `{"ru": "Тест", "en": "Test"}`
  - `async def create_attribute_value(client, attribute_id, *, code="test_val", slug="test-val", value_i18n=None) -> dict` -- POST /api/v1/catalog/attributes/{attribute_id}/values, returns response JSON (has `id` field). Default value_i18n: `{"ru": "Значение", "en": "Value"}`
  - `async def create_attribute_template(client, *, code="test_template", name_i18n=None) -> dict` -- POST /api/v1/catalog/attribute-templates, returns response JSON (has `id` field). Default name_i18n: `{"ru": "Шаблон", "en": "Template"}`
  - `async def bind_attribute_to_template(client, template_id, attribute_id, *, requirement_level="optional") -> dict` -- POST /api/v1/catalog/attribute-templates/{template_id}/attributes, returns response JSON (has `id` field)

  Each helper should:
  - Accept `**kwargs` for overriding any field
  - Use a unique slug/code suffix based on `uuid.uuid4().hex[:8]` to avoid conflicts between tests
  - Assert the response status code is 201 before returning, raising AssertionError with the response body if not
  - Return the full response JSON dict

  3. Add `import uuid` and `from httpx import AsyncClient` at the top.
  4. Add module docstring explaining these are shared helpers for catalog E2E tests.
  5. Add `import pytest` and `pytestmark = pytest.mark.asyncio` at the top.
  </action>
  <verify>python -c "import ast; ast.parse(open('backend/tests/e2e/api/v1/catalog/conftest.py').read()); print('OK')"</verify>
  <acceptance_criteria>
    - backend/tests/e2e/api/v1/catalog/__init__.py exists (empty or docstring-only)
    - backend/tests/e2e/api/v1/catalog/conftest.py contains "async def create_brand"
    - backend/tests/e2e/api/v1/catalog/conftest.py contains "async def create_category"
    - backend/tests/e2e/api/v1/catalog/conftest.py contains "async def create_attribute"
    - backend/tests/e2e/api/v1/catalog/conftest.py contains "async def create_attribute_value"
    - backend/tests/e2e/api/v1/catalog/conftest.py contains "async def create_attribute_template"
    - backend/tests/e2e/api/v1/catalog/conftest.py contains "async def bind_attribute_to_template"
    - backend/tests/e2e/api/v1/catalog/conftest.py contains "uuid.uuid4().hex"
  </acceptance_criteria>
  <done>Catalog test directory created with conftest.py containing 6 helper functions</done>
</task>

<task type="auto">
  <name>Task 2: Write Brand and Category endpoint E2E tests</name>
  <files>backend/tests/e2e/api/v1/catalog/test_brands.py, backend/tests/e2e/api/v1/catalog/test_categories.py</files>
  <read_first>backend/src/modules/catalog/presentation/router_brands.py, backend/src/modules/catalog/presentation/router_categories.py, backend/src/modules/catalog/presentation/schemas.py, backend/tests/e2e/api/v1/test_brands.py, backend/tests/e2e/api/v1/test_categories.py</read_first>
  <action>
  **test_brands.py** -- Test all 6 brand endpoints:

  ```
  class TestBrandEndpoints:
      # POST /api/v1/catalog/brands
      async def test_create_brand_success(admin_client, db_session):
          payload = {"name": "Nike", "slug": "nike-test-xxx", "logoUrl": "https://cdn.example.com/nike.webp"}
          resp = await admin_client.post("/api/v1/catalog/brands", json=payload)
          assert resp.status_code == 201
          data = resp.json()
          assert "id" in data  # BrandCreateResponse has `id` field

      async def test_create_brand_duplicate_slug_returns_409(admin_client, db_session):
          # Create first, then try same slug
          slug = f"dup-{uuid4().hex[:8]}"
          await admin_client.post("/api/v1/catalog/brands", json={"name": "A", "slug": slug})
          resp = await admin_client.post("/api/v1/catalog/brands", json={"name": "B", "slug": slug})
          assert resp.status_code == 409
          assert resp.json()["error"]["code"]  # Error envelope present

      async def test_create_brand_invalid_slug_returns_422(admin_client, db_session):
          resp = await admin_client.post("/api/v1/catalog/brands", json={"name": "X", "slug": "INVALID SLUG"})
          assert resp.status_code == 422

      # POST /api/v1/catalog/brands/bulk
      async def test_bulk_create_brands_success(admin_client, db_session):
          items = [{"name": f"B{i}", "slug": f"bulk-b{i}-{uuid4().hex[:6]}"} for i in range(3)]
          resp = await admin_client.post("/api/v1/catalog/brands/bulk", json={"items": items, "skipExisting": False})
          assert resp.status_code == 201
          data = resp.json()
          assert data["createdCount"] == 3
          assert len(data["ids"]) == 3

      # GET /api/v1/catalog/brands
      async def test_list_brands_success(admin_client, db_session):
          # Create a brand first
          await create_brand(admin_client)
          resp = await admin_client.get("/api/v1/catalog/brands", params={"offset": 0, "limit": 10})
          assert resp.status_code == 200
          data = resp.json()
          assert "items" in data
          assert "total" in data
          assert "offset" in data
          assert "limit" in data
          assert "hasNext" in data
          assert len(data["items"]) >= 1
          # Check camelCase field names on first item
          item = data["items"][0]
          assert "id" in item
          assert "name" in item
          assert "slug" in item

      # GET /api/v1/catalog/brands/{brand_id}
      async def test_get_brand_success(admin_client, db_session):
          created = await create_brand(admin_client)
          resp = await admin_client.get(f"/api/v1/catalog/brands/{created['id']}")
          assert resp.status_code == 200
          data = resp.json()
          assert data["id"] == created["id"]

      async def test_get_brand_not_found_returns_404(admin_client, db_session):
          fake_id = str(uuid4())
          resp = await admin_client.get(f"/api/v1/catalog/brands/{fake_id}")
          assert resp.status_code == 404

      # PATCH /api/v1/catalog/brands/{brand_id}
      async def test_update_brand_success(admin_client, db_session):
          created = await create_brand(admin_client)
          resp = await admin_client.patch(f"/api/v1/catalog/brands/{created['id']}", json={"name": "Updated"})
          assert resp.status_code == 200
          assert resp.json()["name"] == "Updated"

      # DELETE /api/v1/catalog/brands/{brand_id}
      async def test_delete_brand_success(admin_client, db_session):
          created = await create_brand(admin_client)
          resp = await admin_client.delete(f"/api/v1/catalog/brands/{created['id']}")
          assert resp.status_code == 204

      async def test_delete_brand_not_found_returns_404(admin_client, db_session):
          resp = await admin_client.delete(f"/api/v1/catalog/brands/{uuid4()}")
          assert resp.status_code == 404
  ```

  **test_categories.py** -- Test all 7 category endpoints:

  ```
  class TestCategoryEndpoints:
      # POST /api/v1/catalog/categories
      async def test_create_category_success(admin_client, db_session):
          payload = {"nameI18n": {"ru": "Обувь", "en": "Shoes"}, "slug": f"shoes-{uuid4().hex[:8]}"}
          resp = await admin_client.post("/api/v1/catalog/categories", json=payload)
          assert resp.status_code == 201
          data = resp.json()
          assert "id" in data
          assert "message" in data

      async def test_create_category_missing_locale_returns_422(admin_client, db_session):
          resp = await admin_client.post("/api/v1/catalog/categories", json={"nameI18n": {"en": "Only English"}, "slug": "bad-cat"})
          assert resp.status_code == 422  # Missing required "ru" locale

      async def test_create_category_with_parent(admin_client, db_session):
          parent = await create_category(admin_client, slug=f"parent-{uuid4().hex[:8]}")
          child_resp = await admin_client.post("/api/v1/catalog/categories", json={
              "nameI18n": {"ru": "Ребенок", "en": "Child"},
              "slug": f"child-{uuid4().hex[:8]}",
              "parentId": parent["id"]
          })
          assert child_resp.status_code == 201

      # POST /api/v1/catalog/categories/bulk
      async def test_bulk_create_categories_success(admin_client, db_session):
          items = [{"nameI18n": {"ru": f"Кат{i}", "en": f"Cat{i}"}, "slug": f"bulk-cat{i}-{uuid4().hex[:6]}"} for i in range(3)]
          resp = await admin_client.post("/api/v1/catalog/categories/bulk", json={"items": items})
          assert resp.status_code == 201
          data = resp.json()
          assert data["createdCount"] == 3

      # GET /api/v1/catalog/categories/tree
      async def test_get_category_tree_success(admin_client, db_session):
          await create_category(admin_client)
          resp = await admin_client.get("/api/v1/catalog/categories/tree")
          assert resp.status_code == 200
          assert isinstance(resp.json(), list)

      # GET /api/v1/catalog/categories
      async def test_list_categories_success(admin_client, db_session):
          await create_category(admin_client)
          resp = await admin_client.get("/api/v1/catalog/categories", params={"offset": 0, "limit": 10})
          assert resp.status_code == 200
          data = resp.json()
          assert "items" in data and "total" in data

      # GET /api/v1/catalog/categories/{id}
      async def test_get_category_success(admin_client, db_session):
          created = await create_category(admin_client)
          resp = await admin_client.get(f"/api/v1/catalog/categories/{created['id']}")
          assert resp.status_code == 200
          data = resp.json()
          assert data["id"] == created["id"]
          assert "nameI18n" in data
          assert "slug" in data
          assert "fullSlug" in data
          assert "level" in data

      # PATCH /api/v1/catalog/categories/{id}
      async def test_update_category_success(admin_client, db_session):
          created = await create_category(admin_client)
          resp = await admin_client.patch(f"/api/v1/catalog/categories/{created['id']}", json={"sortOrder": 5})
          assert resp.status_code == 200

      # DELETE /api/v1/catalog/categories/{id}
      async def test_delete_category_success(admin_client, db_session):
          created = await create_category(admin_client)
          resp = await admin_client.delete(f"/api/v1/catalog/categories/{created['id']}")
          assert resp.status_code == 204
  ```

  Use `from tests.e2e.api.v1.catalog.conftest import create_brand, create_category` (or rely on conftest auto-discovery). Import `uuid4` from `uuid`. Add `pytestmark = pytest.mark.asyncio` at top.
  </action>
  <verify>cd backend && python -m pytest tests/e2e/api/v1/catalog/test_brands.py tests/e2e/api/v1/catalog/test_categories.py -x -q --timeout=30 2>&1 | tail -5</verify>
  <acceptance_criteria>
    - test_brands.py contains "test_create_brand_success"
    - test_brands.py contains "test_create_brand_duplicate_slug_returns_409"
    - test_brands.py contains "test_list_brands_success"
    - test_brands.py contains "test_delete_brand_success"
    - test_brands.py contains "test_bulk_create_brands_success"
    - test_categories.py contains "test_create_category_success"
    - test_categories.py contains "test_create_category_missing_locale_returns_422"
    - test_categories.py contains "test_get_category_tree_success"
    - test_categories.py contains "test_delete_category_success"
    - test_categories.py contains "test_bulk_create_categories_success"
  </acceptance_criteria>
  <done>Brand tests (10 tests) and Category tests (9 tests) pass</done>
</task>

<task type="auto">
  <name>Task 3: Write Attribute, AttributeValue, and AttributeTemplate endpoint E2E tests</name>
  <files>backend/tests/e2e/api/v1/catalog/test_attributes.py, backend/tests/e2e/api/v1/catalog/test_attribute_values.py, backend/tests/e2e/api/v1/catalog/test_attribute_templates.py</files>
  <read_first>backend/src/modules/catalog/presentation/router_attributes.py, backend/src/modules/catalog/presentation/router_attribute_values.py, backend/src/modules/catalog/presentation/router_attribute_templates.py, backend/src/modules/catalog/presentation/schemas.py</read_first>
  <action>
  **test_attributes.py** -- Test all 7 attribute endpoints:

  Tests to write:
  - test_create_attribute_success: POST /catalog/attributes -> 201, response has `id`
  - test_create_attribute_invalid_data_type_returns_422: invalid data_type -> 422
  - test_bulk_create_attributes_success: POST /catalog/attributes/bulk -> 201, createdCount matches
  - test_list_attributes_success: GET /catalog/attributes -> 200, PaginatedResponse shape (items, total, offset, limit, hasNext)
  - test_get_attribute_success: GET /catalog/attributes/{id} -> 200, camelCase fields (code, slug, nameI18n, dataType, uiType, isDictionary, level, isFilterable, isSearchable, searchWeight, isComparable, isVisibleOnCard)
  - test_get_attribute_not_found_returns_404: fake UUID -> 404
  - test_update_attribute_success: PATCH /catalog/attributes/{id} -> 200
  - test_delete_attribute_success: DELETE /catalog/attributes/{id} -> 204
  - test_get_attribute_usage_success: GET /catalog/attributes/{id}/usage -> 200, response has templateCount, categoryCount, productCount

  **test_attribute_values.py** -- Test all 9 attribute value endpoints:

  Tests to write:
  - test_add_attribute_value_success: POST /catalog/attributes/{id}/values -> 201
  - test_add_attribute_value_missing_locale_returns_422: missing "ru" -> 422
  - test_bulk_add_attribute_values_success: POST /catalog/attributes/{id}/values/bulk -> 201
  - test_list_attribute_values_success: GET /catalog/attributes/{id}/values -> 200, PaginatedResponse
  - test_get_attribute_value_success: GET /catalog/attributes/{aid}/values/{vid} -> 200, fields: id, attributeId, code, slug, valueI18n, searchAliases, metaData, sortOrder, isActive
  - test_update_attribute_value_success: PATCH /catalog/attributes/{aid}/values/{vid} -> 200
  - test_deactivate_attribute_value_success: PATCH /catalog/attributes/{aid}/values/{vid}/deactivate -> 200, isActive=false
  - test_activate_attribute_value_success: PATCH /catalog/attributes/{aid}/values/{vid}/activate -> 200, isActive=true
  - test_delete_attribute_value_success: DELETE /catalog/attributes/{aid}/values/{vid} -> 204
  - test_reorder_attribute_values_success: POST /catalog/attributes/{aid}/values/reorder -> 204

  **test_attribute_templates.py** -- Test all 11 attribute template + binding endpoints:

  Tests to write:
  - test_create_template_success: POST /catalog/attribute-templates -> 201, has id
  - test_clone_template_success: POST /catalog/attribute-templates/clone -> 201, has id and bindingsCopied
  - test_list_templates_success: GET /catalog/attribute-templates -> 200, PaginatedResponse
  - test_get_template_success: GET /catalog/attribute-templates/{id} -> 200, fields: id, code, nameI18n, descriptionI18n, sortOrder
  - test_update_template_success: PATCH /catalog/attribute-templates/{id} -> 200
  - test_delete_template_success: DELETE /catalog/attribute-templates/{id} -> 204
  - test_bind_attribute_success: POST /catalog/attribute-templates/{id}/attributes -> 201, has id and affectedCategoriesCount
  - test_list_bindings_success: GET /catalog/attribute-templates/{id}/attributes -> 200, PaginatedResponse with enriched fields
  - test_update_binding_success: PATCH /catalog/attribute-templates/{tid}/attributes/{bid} -> 200
  - test_unbind_attribute_success: DELETE /catalog/attribute-templates/{tid}/attributes/{bid} -> 204
  - test_reorder_bindings_success: POST /catalog/attribute-templates/{tid}/attributes/reorder -> 204

  Each test file: `import pytest`, `pytestmark = pytest.mark.asyncio`, use helpers from conftest.
  Each test uses unique slugs/codes with uuid suffix to avoid inter-test conflicts.
  For nested endpoints (values, bindings), first create the parent entity via helper.
  </action>
  <verify>cd backend && python -m pytest tests/e2e/api/v1/catalog/test_attributes.py tests/e2e/api/v1/catalog/test_attribute_values.py tests/e2e/api/v1/catalog/test_attribute_templates.py -x -q --timeout=30 2>&1 | tail -5</verify>
  <acceptance_criteria>
    - test_attributes.py contains "test_create_attribute_success"
    - test_attributes.py contains "test_get_attribute_usage_success"
    - test_attributes.py contains "test_delete_attribute_success"
    - test_attribute_values.py contains "test_add_attribute_value_success"
    - test_attribute_values.py contains "test_deactivate_attribute_value_success"
    - test_attribute_values.py contains "test_reorder_attribute_values_success"
    - test_attribute_templates.py contains "test_create_template_success"
    - test_attribute_templates.py contains "test_bind_attribute_success"
    - test_attribute_templates.py contains "test_clone_template_success"
    - test_attribute_templates.py contains "test_reorder_bindings_success"
  </acceptance_criteria>
  <done>Attribute tests (9), AttributeValue tests (10), and AttributeTemplate tests (11) pass</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd backend && python -m pytest tests/e2e/api/v1/catalog/ -x -q --timeout=30` passes all tests
- [ ] Each test file has at least one happy-path and one error-path test
- [ ] All responses assert camelCase field names (not snake_case)
- [ ] PaginatedResponse shape verified: items, total, offset, limit, hasNext
</verification>

<success_criteria>

- All tasks completed
- All 5 test files created with comprehensive coverage of 40 endpoints
- Shared conftest.py with 6 helper functions working correctly
- No test failures or import errors
- Response shape assertions verify camelCase field names

</success_criteria>

<output>
After completion, create `.planning/phases/08-api-contract-validation/08-01-SUMMARY.md`
</output>
