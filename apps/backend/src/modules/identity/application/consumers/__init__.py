"""Async event consumers for the Identity module (application layer).

Contains TaskIQ-based consumers triggered via the Outbox Relay pattern
for handling domain events asynchronously (e.g., cache invalidation).
"""
