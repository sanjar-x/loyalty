"""Application bootstrap package (composition root).

Contains factories and configuration for assembling the application:
web server, background worker, scheduler, DI container, broker, and
structured logging setup.
"""
