"""Infrastructure layer: cross-cutting concerns and technology adapters.

Provides concrete implementations for ports defined in the shared/domain
layers -- database access, caching, security, object storage, logging,
and the transactional outbox pattern.
"""
