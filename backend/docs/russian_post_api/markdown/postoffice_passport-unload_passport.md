# Выгрузка из паспорта ОПС

Выгружает данные ОПС, ПВЗ, Почтоматов из Паспорта ОПС.

Генерирует и возвращает zip архив с текстовым файлом TYPEdd_MMMM_yyyy.txt, где:

- TYPE -- тип объекта
- dd_MMMM_yyyy -- время создания архива

## Параметры запроса

| Параметр | Значение |
|----------|----------|
| Локальный URL | **/1.0/unloading-passport/zip?type={type}** |
| Тип | **GET** |

**Query-параметры:**

| Параметр | Описание |
|----------|----------|
| type | Тип объекта |

## Заголовки запроса

| Заголовок | Значение |
|-----------|----------|
| **Authorization** | **AccessToken {token}** |
| **X-User-Authorization** | **Basic {key}** |
| **Accept** | **application/octet-stream** |
